# D-053 — BRIEF-008 resolved: Path A adopted — early-career, low-complexity segment as the sole MVP founding target; all others parked, not discarded

- **Tier:** 3 — resolves BRIEF-008 (both trigger 3, bears on the P2 teach-never-advise line via the
  debt-heavy/reactive candidate, and trigger 6, unclassifiable/multi-category, had fired). Owner-decided
  directly in conversation.
- **Decision:** **Path A adopted.** The segment both independently-run lenses (Product, Business/GTM)
  converged on — early-career, single-income-stream, low-financial-complexity salaried users — is now
  FinTutor's sole founding target for MVP design work and Phase-3 (private structured testing) recruitment.
  All other BRIEF-008 candidates (fragmented multi-holding; variable/gig income; debt-heavy/reactive;
  household financial coordinator; dual-income households; first-jobbers; NRIs) are **explicitly PARKED for
  post-MVP phases, not discarded** — same direction-vs-build split pattern D-031 used for real estate/cash/
  alternatives. Path C (formally excluding the two structurally-unsupported segments now) was **not**
  separately adopted — owner's framing ("keep them open... not completely discarding") reads as deliberately
  not closing any door yet, beyond narrowing the founding target itself.
- **What this quietly resolves:** the debt-heavy/reactive segment's P2 stress-test question (BRIEF-008's
  "what only the owner can judge" #1) is answered as a side effect — it isn't in the founding population, so
  it isn't tested early. Whether to test it in a later wave is still open, just no longer urgent.
- **What this does NOT resolve, still open:** whether `PROJECT_SPEC.md` §3's "not defined by age; defined by
  intent" framing should be rewritten (BRIEF-008's "what only the owner can judge" #3) — not addressed in
  this decision, not silently changed. See dependency flag below.
- **New work queued, not yet done:** owner flagged that "early-career, low-complexity" is itself still broad
  and asked for it to be further subdivided into more specific sub-segments. This is the immediate next
  piece of work on the same underlying question (still "who are we designing for"), not a new strategic
  topic — method (reuse the two-independent-lens approach, or a single direct pass) to be confirmed with the
  owner before starting.
- **Dependency flag:** `PROJECT_SPEC.md` §3 needs a proposed rewrite reflecting this decision (per
  `CLAUDE.md`'s file-permission rule — propose, confirm, then apply, not silent). Proposed text pending in
  the same conversation this decision was made in.
- **Reversibility:** Medium — no code or screens exist yet against this segment (`app/` has only the bare
  BQ-014 skeleton), so nothing built has to be undone; but real onboarding/capture design work is about to
  start being shaped around this population, so the cost of reversing grows quickly from here, unlike a
  pre-any-design decision.
- **Date:** 03-Aug-2026
