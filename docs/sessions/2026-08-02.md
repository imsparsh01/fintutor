# Session — 02-Aug-2026

**Task:** Retire the two-home model (laptop/Claude Code = build, separate Claude Project = think) in favor
of a single unified home, now that Cowork is pointed directly at this repo.

**What changed:**
- Logged **D-033** in `docs/DECISION_LOG.md` (Tier 3, owner decision made directly in conversation): the
  laptop/Claude Project split is retired. One repo, one tool, covering both build and strategy/compliance
  work. The discipline the split protected (bounded execution kept separate from deliberate decisions) is
  preserved but now self-enforced via `docs/DECISION_PROTOCOL.md`'s tiers and `CLAUDE.md`'s file-permission
  lanes, rather than by a technical access boundary.
- `PROJECT_SPEC.md`: rewrote the ORIENTATION block to describe the single-home model; added a v2.2 change
  log entry.
- `CLAUDE.md`: dropped the "two homes" framing and the "you do not have access to the Claude Project" line;
  reframed the "never edit" file lane (`PROJECT_GOVERNANCE.md`, `docs/DECISION_PROTOCOL.md`,
  `HOW_TO_RUN_THIS_PROJECT.md`) as "deliberate-only — requires an explicit owner-confirmed decision," since
  those files are now technically reachable and the guardrail has to be procedural, not physical. Dropped the
  end-of-session "re-upload to the Project" step (nothing to re-upload).
- `PROJECT_GOVERNANCE.md`: retitled from "operating charter for the Claude Project" to "operating charter for
  strategy and decision-making sessions"; removed the "keeping the two homes in sync" section; kept decision
  discipline, anti-patterns, and standing principles unchanged (never Project-specific in substance).
- `HOW_TO_RUN_THIS_PROJECT.md`, `README.md`, `docs/BUILD_QUEUE.md`: light edits removing stale two-home
  references (e.g. "this file lives only on the laptop, not uploaded to the Claude Project").
- `AGENTS.md`: mirrored the same rewrite (it's a near-duplicate of `CLAUDE.md` written for a different
  agent — kept consistent rather than left contradicting the updated `CLAUDE.md`).

**Not changed:** `docs/DECISION_PROTOCOL.md` — no two-home framing found in it (grepped, confirmed clean),
so it didn't need edits. Historical entries in `DECISION_LOG.md` and past `docs/sessions/*.md` files were
left untouched (append-only discipline — history isn't rewritten even when it describes a retired model).

**What's next:** `docs/BUILD_QUEUE.md` READY is still empty (BQ-004 remains BLOCKED on the backend `deepen`
selection rule). This session didn't touch product/build work — it was purely the operating-model change
requested by the owner. Owner should skim the D-033 entry and the rewritten `CLAUDE.md`/`PROJECT_GOVERNANCE.md`
to confirm the new framing reads the way they intended before the next build session runs under it.
