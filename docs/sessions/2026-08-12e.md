# Session 2026-08-12e

Shipped BQ-062 / D-112. Portfolio Health and the backend 80C-room calculation now use the same strict
cadence gate: missing or unrecognised premium cadence contributes zero instead of being guessed monthly.
Added eight explicit six-month spellings; each converts to two payments yearly, and the backend budget
uses the corresponding monthly amount ÷ 6.

Verification: TypeScript clean; backend Python compile clean; Expo web export clean; the actual compiled
frontend `computeSubScores` and backend `compute_tax_saving_room` passed the same cadence matrix covering
monthly, quarterly, six-month, annual, weekly, blank, unknown, and null. All eight six-month aliases were
also checked directly on both sides. Removed the two now-resolved entries from `KNOWN_LIMITATIONS.md`.

No schema, dependency, 80C-cap, eligibility-category, or tax-regime change. Build queue is empty again.
