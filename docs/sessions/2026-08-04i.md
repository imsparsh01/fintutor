# Session 2026-08-04i

**What:** Fresh session, picked up from 2026-08-04h's close. Two decisions resolved two standing backlog
items, a large live-verification pass surfaced real gaps, then two of those gaps got closed same-session.
Five decisions logged, four BQ items shipped, two briefs written.

**D-072 + BQ-004 — BRIEF-006 fully resolved.** Owner confirmed Path A (a narrow Haiku classifier) for the
general Chat-tab `deepen` case — the piece D-071 deliberately left open. Built
`backend/app/services/deepen_classifier.py`: reads the question + holdings (alias/product_type only,
never `display_name`), returns a single confident alias or nothing; runs only when D-071's UI-signal path
didn't already set `deepen`; any ambiguity, unrecognized reply, API error, or missing key degrades to
D-028's existing safe default. First real use of D-002's Haiku half. This closes BRIEF-006 entirely — every
`/chat` entry point now has a decided mechanism.

**BRIEF-017 + D-073 + BQ-035 — variable-income budgeting resolved.** Picked up BRIEF-011's escalated
hard-stop. Modeled three real paths (rolling-window average / declared floor+typical / defer), gave an
explicit lean when asked (Path B2 — cheap, no migration since `Income.sources` is already JSONB, and
doesn't compound the standing unverified-surface-area risk the way a new object+capture-flow would).
Owner confirmed. Built: `Income.sources` items gain optional `amount_high`; `amount` keeps its existing
role in `compute_budget()` unchanged (the floor); `amount_high` is purely informational. Both figures shown
in the UI, a caption clarifies which one the budget uses.

**The comprehensive live-verification pass — the biggest piece of this session.** Owner asked to close the
standing "nothing's been run in a real app" risk as thoroughly as possible. Backend: spun up a real local
Postgres, ran every migration for the first time inside a session, started the real server, and hit all 25
routes with real data — 36/36 hand-checked assertions passed. Frontend: this sandbox has no Docker, no
working iOS Simulator, no Android SDK, and blocks all outbound calls to `supabase.co` (confirmed via the
proxy's own status log — a deliberate policy denial, reported rather than routed around). Got a real
Chromium browser running the actual app anyway: mocked only the Supabase auth response locally (request
never left the browser), then drove the rest for real — onboarding, all three holding-family tabs,
Budgeting (including live confirmation that D-073's new field works end to end), Chat, Holding Detail, the
loan-vs-invest comparison (matched the backend test exactly), and the "Ask about this" alias-only prefill.

**Two real findings, logged not fixed (owner's explicit instruction — log and prioritize, don't pick yet):**
- **CRITICAL** — no way to add a new holding anywhere in the app, manually or AI-surfaced. A silent hole in
  D-059's coverage (editing depth was decided, creation never was).
- **MEDIUM** — no discretionary-category CRUD exists, so that budget line could never be non-zero.
Two smaller LOW items (a stale comment in `consolidated.py`, Chat's raw error message) went straight into
`docs/KNOWN_LIMITATIONS.md`. All local test infrastructure (Postgres, backend server, the temporarily
`npm install`-ed `react-dom`/`react-native-web`, test credentials) was fully cleaned up and reverted after.

**BRIEF-018 + D-074 + BQ-036 — the CRITICAL finding closed same session.** Ran the actual trigger checklist
explicitly rather than assuming — no hard trigger fires (D-012 already named the manual fallback as
required MVP; this executes that, doesn't reinterpret it), so this routed to Tier 2, not Tier 3 like the
recent run of briefs. Modeled the real fork: does the create form ever expose "alias" to the user, or does
the backend auto-generate one? Gave a lean when asked (Path A, not close) — aliases were always meant to
be backend-internal (D-010/D-011), and Path B's duplicate-alias collision is a real first-time-user
failure, not hypothetical. Owner confirmed. Built: `POST /holdings`'s `alias` is now optional (backend
generates `"{Humanized Product Type}-{n}"` when omitted); `HoldingEditModal` extended with a create mode
(null `holding`) — no alias field, product-type picker scoped to the current family tab; edit mode
completely unchanged. **Verified live, not just type-checked** — same real Postgres/backend/browser rig,
specifically to close the loop: tapped "+ Add loan," confirmed the scoped picker and the total absence of
an alias field, saved, and confirmed via a direct backend query that the holding landed with
`alias: "Personal Loan-1"` exactly as designed. Edit mode separately re-confirmed unaffected.

**BQ-037 — discretionary-category CRUD, the MEDIUM finding closed same session.** No decision needed — same
already-decided-shape CRUD gap as Income/Goals (BQ-016/017), not a design question. `GET`/
`POST /discretionary-categories` + a new "Discretionary spending" section on `BudgetingScreen.tsx`,
matching the existing Income/Goals form pattern. Verified via type-check + bundle + a backend unit test
only, not the full live rig — proportionate to this being genuinely mechanical, same bar BQ-016/017
originally got.

**Every backend item this session verified via `python -m py_compile` + route registration + targeted unit
tests against mocked sessions; every frontend item via `npx tsc --noEmit` + `npx expo export --platform
android` (933→934 modules across the session, no errors). The live-verification pass and the two same-session
fixes additionally got real Postgres/real-server/real-browser confirmation** — a first for this project.

**`docs/DECISION_LOG.md` changed substantially this session — five new decisions (D-072 through D-074, plus
the two already covered above).** `PROJECT_SPEC.md` was not touched.

**What's next:** `docs/BUILD_QUEUE.md`'s READY and BLOCKED are both empty. Standing backlog, unchanged in
substance: the data privacy policy (D-010), a real device/simulator pass now that this session closed as
much of that gap as this sandbox structurally allows, legal review scheduling, the UX principles section,
and the two LOW findings sitting in `KNOWN_LIMITATIONS.md` (stale comment, raw Chat error message) whenever
someone's next touching those files anyway.
