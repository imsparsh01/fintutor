# Session — 02-Aug-2026 (fifth session)

**Task:** Get live API test automation working after the sandbox blocked direct calls, then run and score
BQ-007.

**What changed:**
- Diagnosed the earlier "Unauthorized" failures properly: the build sandbox's outbound proxy blocks any
  request to `api.anthropic.com` carrying an API-key header (confirmed by comparing a keyed vs. unkeyed
  request — the unkeyed one reaches Anthropic's real API and gets a genuine JSON 401; the keyed one gets
  intercepted by the sandbox's own MITM proxy and returns a bare-text 401 first). Checked the MCP connector
  registry for a sanctioned alternative — none exists. This is a deliberate environment restriction, not a
  bad key, and not something to route around from inside the sandbox.
- Wrote `scripts/run_phase1_test.py` — stdlib-only Python, runs on the owner's own machine (no such
  restriction there), calls the Anthropic API directly, saves raw outputs into the shared repo folder.
  Pre-configured defaults match BQ-007; overridable via flags for future test runs.
- Owner ran it locally: 5 fresh calls, `v0.7` + `FIXTURE_user_01.json` + Q1, all succeeded.
- Read and scored all 5 outputs against `TEST_PROTOCOL.md`'s Q1 checklist. Results in new
  `docs/PHASE1_RUN5_RESULTS.md`. **FINDING 10 confirmed closed (0/5).** **FINDING 9 at 1/5 (20%, down from
  2/5, n too small to call improved).** **FINDING 11 (new): "worth [X]" framing on Card-1 mentions in 4/5
  runs, with the prompt's own named-forbidden phrase ("worth having in view") reproduced verbatim in 2/5** —
  found by applying the full checklist, not something BQ-007 was specifically scoped to look for.
- `docs/BUILD_QUEUE.md`: BQ-007 moved to DONE with full notes; READY empty again.
- `PROJECT_SPEC.md` §8: FINDING 9 entry updated with the new rate; FINDING 11 added as a new open item.

**Why FINDING 11 isn't resolved here:** it's a §3 rule 5 (compliance-category) recurrence — the exact
territory D-034's autonomy grant carves out. It needs a brief and an owner decision, same as FINDING 9/10 did,
not a silent prompt patch.

**What's next:** FINDING 11 is the freshest, highest-signal open item (a named-forbidden phrase reproducing
verbatim in 40% of runs is hard to read as noise) — worth a brief soon. FINDING 9 remains open and untouched
by this session's evidence either way. The local test-runner script now covers any future Phase 1 test round,
not just BQ-007 — the sandbox limitation that made this necessary is now documented and worked around
cleanly (owner runs the script, Claude reads and scores the output) rather than needing rediscovery each
time.
