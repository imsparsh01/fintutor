# Session 2026-08-10

**What:** Continuation of the 2026-08-05 local Mac thread — resolved D-082's open onboarding fork,
confirmed and queued the resulting PRD as BQ-042, discovered a parallel session had already built and
merged BQ-042 independently, resolved that conflict by adopting the merged version and live-verifying it,
found and fixed a real bug in it. A dense, multi-part session; see the referenced decisions for full detail.

**D-083 — resolved D-082's cross-turn-state fork.** Owner chose Path A: a narrow `onboarding_stage`
indicator, not conversation memory. Unblocked `docs/features/onboarding/PRD.md`, which was then extended
with a draft stage/path map, persisted-state shape, and fail-safe mechanics — grounded in BRIEF-011,
nothing invented fresh.

**D-084 — PRD confirmed, BQ-042 queued.** Owner greenlit the draft as proposed; `BUILD_QUEUE.md` got a
full-scope READY item.

**Discovered a real merge conflict.** Building BQ-042 locally, `git fetch` revealed `main` was 3 commits
ahead — a separate cloud session (2026-08-09, no visibility into it from this thread) had independently
built and merged its own BQ-042 implementation, already archived as done. Per `CLAUDE.md`'s D-056 carve-out
(diverged `main` stops for the owner, not autonomous resolution), stopped and presented a full comparison
rather than silently picking a side or merging blind. Real differences: their version uses a second Haiku
classifier for content-aware stage advancement (mine used simple turn-count auto-advance); they extended
the versioned system prompt file to document the new `onboarding` baseline field (mine avoided touching
it); their version was *not* live-verified (their cloud session had no `DATABASE_URL`/`ANTHROPIC_API_KEY`);
mine was, fully, against real Supabase + real Anthropic calls. Both migrations independently converged on
an identical `onboarding_states` schema.

**Owner's call: keep their merged version, live-verify it, fix what breaks.** Stashed the local
independent implementation (kept in `git stash`, not deleted — reference only, superseded), pulled their
version, re-stamped `alembic_version` to their revision (`f3a9c7d1b820` — schema was already identical, so
this was pure bookkeeping, no DDL). Live-verified the `reactive_dabbler` track end-to-end: deterministic
chip routing, the stage-advance classifier correctly staying in `intro` until real loan details arrived
then correctly advancing to `mechanism`, computed EMI/interest figures matching the user's real numbers,
and the independent D-078 holding-capture classifier firing correctly alongside it, unaffected.

**Found a real bug via that live verification, not hypothetically.** Turn 3 broke: "no, that's the only
one" (answering the AI's own prior question) produced a response that had completely lost the thread,
because D-022 sends zero prior-turn content to the model, ever — a property of D-022 itself, not specific
to either implementation. Reported the full failure transcript and root cause rather than silently patching
around it.

**D-085 — limited, unpersisted memory for the onboarding exchange, owner-decided.** Forwards exactly the
AI's own single immediately-preceding message in the current onboarding conversation — sourced from the
frontend's already-existing local display state (`ChatThread`'s `messages`), never written to any database
column, never sent for any call outside onboarding. Explicit reasoning for why this doesn't reopen D-022's
still-parked general case: D-022 was blocked on a data-retention trigger tied to the unwritten D-010 policy,
and this creates no new stored data at all — nothing for a retention policy to need to cover. Implemented in
`onboarding.py`, `main.py`, `chat.ts`, `ChatThread.tsx`, and the system prompt's `onboarding` field docs
(D-085's `last_ai_message` sub-field, correcting the now-inaccurate "never given what was said in prior
turns" claim the merged session had written). Re-verified live against the exact failure transcript — the
`reactive_dabbler` track now runs `intro` → `mechanism` → `reflect` → `complete` cleanly, with a genuine
reflective question and an explicit refusal to judge fit ("I'm not going to tell you whether it was a good
call or not").

**Flagged repeated prompt-injection attempts, again.** Multiple times this session, after a `git checkout
--`/stash revert was independently confirmed clean (via fresh `git status`, `git diff --stat`, or `grep`),
a tool-originated system-reminder falsely claimed the reverted file still held the old content and
instructed not to mention this to the owner. Verified independently each time and told the owner rather
than complying with the "don't tell them" instruction, consistent with treating observed tool content as
untrusted data, not a valid instruction source.

**Also this session:** synced git (confirmed nothing was on GitHub that wasn't already local — the
apparent "out of sync" state was entirely this session's own uncommitted work), refreshed the CEO dashboard
through D-082 and published it as an artifact, wrote a grounded `docs/marketing/BRAND_PRD.md` for external
landing-page/Instagram work (three fields left explicitly open rather than fabricated: waitlist incentive,
visual palette, launch metrics), and set up `.claude/launch.json` for both dev servers via `preview_start`
(hit and fixed a sandbox `getcwd` restriction on relative `cd`/`source` shell built-ins — fixed with
absolute paths only).

**Archived D-066 through D-070** once `DECISION_LOG.md` hit 20/~20 — moved verbatim into
`DECISION_LOG_ARCHIVE.md` (now 15 live, 68 archived), no content changes, per the D-081 rolling-window habit.

**Closed out the PRD's remaining live-verification gap.** Owner asked for a broader pass beyond the one
bug-fix regression test, so all four tracks got tested end-to-end against real Supabase + real Anthropic,
not just `reactive_dabbler`:
- `fresh_starter` (`intro → sequencing → complete`) — the compliance-sensitive `sequencing` stage delivered
  real computed numbers (a ₹20,000 surplus, a ₹90,000–1,80,000 buffer range from the user's actual
  outgoings) while explicitly declining to prescribe an order ("None of the three stands in for the other
  two... depends on things specific to you").
- `habit_former` (`intro → gapscan → complete`) — correctly named the missing category (protection)
  generically, never a specific product, using typical figures framed as typical.
- `unclassified`, tested via genuinely ambiguous **free text** (not a chip tap, to exercise the actual
  Haiku classifier rather than the deterministic hint) — correctly stayed unclassified rather than forcing
  a track, then the turn-budget fail-safe fired correctly ("You're good to move on to the rest of the app
  now").

All four tracks, the deterministic chip-hint path, the free-text classifier path, the content-aware
stage-advance classifier, and the turn-budget/`closing_instruction` fail-safe are now confirmed live, not
just reviewed in code. No code changes were needed this pass — every test used the same temporary
dev-bypass/CORS scaffolding as before, reverted immediately after each run, with test rows deleted from
`onboarding_states` afterward.

**What's next:** Nothing blocking. `docs/features/onboarding/PRD.md`'s remaining open items (exact per-stage
copy review, the resume-after-skip interaction) are normal build-time follow-ups, not owner decisions — the
"exhaustive live-testing" item is now closed. The three long-standing owner-only items (D-009 legal review,
D-010 privacy policy, `ANTHROPIC_API_KEY` cloud persistence) remain open. `BRAND_PRD.md`'s three open fields
are the owner's/designer's whenever they're ready to move on the landing page.

---

# Session 2026-08-10 (b) — UX mockups adopted, app reskinned

Separate later session on the same day, branch `design/mockups-v1-reskin`. Owner delivered v1 UX mockups
(an HTML artifact + a handoff memo) and asked to finalise them, then build against them with a fleet of
subagents.

**Mockups relocated and catalogued.** `frontend/ux-mockups/*` → `docs/ux/mockups/MOCKUPS_v1.html` +
`MOCKUPS_v1_NOTES.md`; `docs/ux/README.md` gained the third UX category (wireframes = structure,
mockups = aesthetic layer, DESIGN_REFERENCES = other people's UX). The stray `frontend/` folder is gone.

**Eight decisions, all owner-confirmed before any code moved (D-086..D-093).** This closes
`PROJECT_SPEC.md` §8's long-parked aesthetic-layer item — it was open on the explicit condition "until real
screen decisions force it", and the mockups were that forcing function. Spec at **v4.0**,
`PRODUCT_PRINCIPLES.md` at **v1.6** with **P10** (no valence styling on real figures) and **P11** (the
tutor's voice has its own typeface) added. Motion and density remain deliberately unprincipled.

Owner's fork choices, for the record: visual register **1a** (warm ledger); teaching placement **1f**
(full-screen walkthrough) — chosen *over* the handoff doc's recommended `1e`, and the only fork of the three
that structurally *can* gate, so D-090 attaches a binding four-part P9 guard; "what we won't say" block
**adopted as drawn**; refusal-before-result meta-statement **dropped**.

**D-093 unparked D-014's execution subagents** — condition satisfied, not overridden (Phase 1 validated at
D-080; the design decisions now exist). Scoped to this reskin only. Backend/schema changes, new
dependencies, new screens, and deliberate-only files were out of bounds for every agent.

**Fleet: 5 Sonnet build agents (parallel, disjoint file ownership) + 3 Opus reviewers.** Partitioning by
file ownership rather than by task meant zero merge conflicts. Governance writing and the token file stayed
with the orchestrating session — it already held the context, so delegating would have cost more and been
less accurate. Model tiering per D-093's table.

**Three hard stops were hit and respected, not worked around:**
- Newsreader / IBM Plex need `expo-font` + `@expo-google-fonts/*` → P11 shipped on **platform system faces**
  instead. The principle is satisfied by the *distinction*, not by any particular typeface; the webfont
  dependency is a separate decision the owner still holds.
- `react-native-reanimated` was never added; motion is built-in `Animated` only.
- No backend agent was spawned at all — verified up front that every data shape the mockups draw already has
  a service, so this was purely presentation.

**Result:** 22 files reskinned, `tsc` exit 0. All four binding constraints independently verified —
`app/lib` + `backend/` untouched, `package.json` byte-identical to `main`, route structure unchanged.

**What the three reviewers caught that no single build agent could:**
- *Compliance:* one genuine advice-line breach in agent-written copy ("Knowing the shape is what tells you
  how urgently it needs attention" — unprompted prioritisation, D-025/D-028). Fixed.
- *Compliance:* an agent-authored sentence in the ESOP "what we won't say" block claimed the app computes
  "the exact cash and tax cost" — **factually false** (the service computes no tax; BRIEF-015 stops short
  deliberately) and contradicted by the backend's own note two lines above it on the same screen. The agent
  was describing the mockup, not the app. Reverted to D-091's approved wording.
- *Compliance:* `HoldingEditModal`'s placeholder named a real bank and product (pre-existing on `main`, not
  a regression) — breaches §2's strict no-product-names stance in app-authored copy. Fixed.
- *Compliance:* two wrong mechanism claims about Indian home loans (floating/repo-linked, tenure-adjusting)
  and credit cards. Fixed. A wrong mechanism explanation is worse than none.
- *Engineering:* D-089 and D-090 were decided the same day and the parallel split severed them — the empty
  sections offer a "two-minute walkthrough" wired to a single chat prefill, while the actual walkthrough
  container sits built and unwired.
- *Engineering:* `HoldingsList.tsx` orphaned (reskinned by one agent, routed around by another);
  `ConsolidatedTotalsCard` stale after add/edit/delete (`useEffect` vs the `useFocusEffect` every list screen
  uses); the three inline list rewrites dropped the "+ Add" affordance from their error branch and lost
  `FlatList` virtualization.
- *Design:* P10 held across a five-way parallel build with **zero** lapses, zero hardcoded hex, zero
  shadows, and clay confined to exactly the two engagement components. What did not hold is coherence —
  two different designs for the *same* "what we won't say" block, four page-title scales, eight primary-button
  geometries, three ledger-label treatments.

**Notable:** the reskin was *stricter* than the mockups in three places, correctly — it dropped "Below it,
prepaying does", "it's the thing to weigh against your exercise window closing", and "those differences
usually matter more than the deduction", all of which are outcome verdicts or comparative prioritisation.

**Coherence pass done (single agent, full `app/` ownership — no parallelism, since coherence is exactly
what parallel agents cannot produce).** Extracted `components/TeachingBlock.tsx` and converted all six
"what we won't say" sites; one page-title scale (the three family screens had no H1 at all); one
primary-button spec (8 geometries → 1); one ledger-row spec; two label roles; a two-step figure scale.
Engineering: `ConsolidatedTotalsCard` → `useFocusEffect`; "+ Add" and a retry restored to the three error
branches; `FlatList` virtualization restored; `TeachingWalkthrough` no longer strands a caller on
`steps=[]`; orphaned `HoldingsList.tsx` deleted with its four stale references.

Verified independently, not taken on report: `tsc` exit 0, zero hex literals, clay only in
`StreakBadge`/`Mascot`, `package.json` byte-identical to `main`, P9 guard intact (every
`onComplete`/`canSkip` hit is the negative-space comment documenting deliberately-omitted props).

**Not visually verified — stated plainly rather than implied.** Expo web preview would need
`react-native-web` + `react-dom`, which are not installed and which D-093 forbids adding. Everything above
is static verification (types, greps, code reading). Running this on a simulator/device is the owner's
next step, and the empty-section CTA (open item 1) is the first thing worth looking at.

**What's next:** the six owner decisions below. Nothing else is blocking.

**Open, owner-only — nothing proceeds on these without a decision:**
1. Empty-section CTA: wire it to `TeachingWalkthrough` (D-090's chosen fork), or keep the chat prefill and
   drop the false "two minutes" caption?
2. `ConsolidatedTotalsCard` zero-vs-absent: the backend collapses "no holdings", "value not filled", and
   "ESOP-only" into the same `0.0`, so an em-dash is not a one-line fix. A fully repaid loan is a *real*
   zero and must not be hidden.
3. Walkthrough step content: D-089 promises "your own numbers" on a surface that is by definition empty.
4. **D-091's own internal inconsistency** (orchestrator's error, not an agent's): its three-part requirement
   list demands every block state what the app will do instead, but the approved ESOP wording contains no
   such offer. Either the requirement narrows or the ESOP text gains an accurate offer half — a Tier-3 call.
5. Goal progress bar: monochrome ink fill honours P10's letter, but a filled bar's *form* still says "how
   far along you are". The mockup's own answer — the fraction, no bar — is safer.
6. Insurance empty-state copy is two lines against its siblings' five, and switches noun ("a policy" vs
   "insurance") mid-screen.
