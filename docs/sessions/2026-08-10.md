# Session 2026-08-10

**What:** Continuation of the 2026-08-05 local Mac thread — resolved D-082's open onboarding fork,
confirmed and queued the resulting PRD as BQ-042, discovered a parallel session had already built and
merged BQ-042 independently, resolved that conflict by adopting the merged version and live-verifying it,
found and fixed a real bug in it. A dense, multi-part session; see the referenced decisions for full detail.

**D-083 — resolved D-082's cross-turn-state fork.** Owner chose Path A: a narrow `onboarding_stage`
indicator, not conversation memory. Unblocked `docs/features/onboarding/PRD.md`, which was then extended
with a draft stage/path map, persisted-state shape, and fail-safe mechanics — grounded in BRIEF-011,
nothing invented fresh.

**D-084 — PRD confirmed, BQ-042 queued.** Owner greenlit the draft as proposed; `BUILD_QUEUE.md` got a
full-scope READY item.

**Discovered a real merge conflict.** Building BQ-042 locally, `git fetch` revealed `main` was 3 commits
ahead — a separate cloud session (2026-08-09, no visibility into it from this thread) had independently
built and merged its own BQ-042 implementation, already archived as done. Per `CLAUDE.md`'s D-056 carve-out
(diverged `main` stops for the owner, not autonomous resolution), stopped and presented a full comparison
rather than silently picking a side or merging blind. Real differences: their version uses a second Haiku
classifier for content-aware stage advancement (mine used simple turn-count auto-advance); they extended
the versioned system prompt file to document the new `onboarding` baseline field (mine avoided touching
it); their version was *not* live-verified (their cloud session had no `DATABASE_URL`/`ANTHROPIC_API_KEY`);
mine was, fully, against real Supabase + real Anthropic calls. Both migrations independently converged on
an identical `onboarding_states` schema.

**Owner's call: keep their merged version, live-verify it, fix what breaks.** Stashed the local
independent implementation (kept in `git stash`, not deleted — reference only, superseded), pulled their
version, re-stamped `alembic_version` to their revision (`f3a9c7d1b820` — schema was already identical, so
this was pure bookkeeping, no DDL). Live-verified the `reactive_dabbler` track end-to-end: deterministic
chip routing, the stage-advance classifier correctly staying in `intro` until real loan details arrived
then correctly advancing to `mechanism`, computed EMI/interest figures matching the user's real numbers,
and the independent D-078 holding-capture classifier firing correctly alongside it, unaffected.

**Found a real bug via that live verification, not hypothetically.** Turn 3 broke: "no, that's the only
one" (answering the AI's own prior question) produced a response that had completely lost the thread,
because D-022 sends zero prior-turn content to the model, ever — a property of D-022 itself, not specific
to either implementation. Reported the full failure transcript and root cause rather than silently patching
around it.

**D-085 — limited, unpersisted memory for the onboarding exchange, owner-decided.** Forwards exactly the
AI's own single immediately-preceding message in the current onboarding conversation — sourced from the
frontend's already-existing local display state (`ChatThread`'s `messages`), never written to any database
column, never sent for any call outside onboarding. Explicit reasoning for why this doesn't reopen D-022's
still-parked general case: D-022 was blocked on a data-retention trigger tied to the unwritten D-010 policy,
and this creates no new stored data at all — nothing for a retention policy to need to cover. Implemented in
`onboarding.py`, `main.py`, `chat.ts`, `ChatThread.tsx`, and the system prompt's `onboarding` field docs
(D-085's `last_ai_message` sub-field, correcting the now-inaccurate "never given what was said in prior
turns" claim the merged session had written). Re-verified live against the exact failure transcript — the
`reactive_dabbler` track now runs `intro` → `mechanism` → `reflect` → `complete` cleanly, with a genuine
reflective question and an explicit refusal to judge fit ("I'm not going to tell you whether it was a good
call or not").

**Flagged repeated prompt-injection attempts, again.** Multiple times this session, after a `git checkout
--`/stash revert was independently confirmed clean (via fresh `git status`, `git diff --stat`, or `grep`),
a tool-originated system-reminder falsely claimed the reverted file still held the old content and
instructed not to mention this to the owner. Verified independently each time and told the owner rather
than complying with the "don't tell them" instruction, consistent with treating observed tool content as
untrusted data, not a valid instruction source.

**Also this session:** synced git (confirmed nothing was on GitHub that wasn't already local — the
apparent "out of sync" state was entirely this session's own uncommitted work), refreshed the CEO dashboard
through D-082 and published it as an artifact, wrote a grounded `docs/marketing/BRAND_PRD.md` for external
landing-page/Instagram work (three fields left explicitly open rather than fabricated: waitlist incentive,
visual palette, launch metrics), and set up `.claude/launch.json` for both dev servers via `preview_start`
(hit and fixed a sandbox `getcwd` restriction on relative `cd`/`source` shell built-ins — fixed with
absolute paths only).

**Archived D-066 through D-070** once `DECISION_LOG.md` hit 20/~20 — moved verbatim into
`DECISION_LOG_ARCHIVE.md` (now 15 live, 68 archived), no content changes, per the D-081 rolling-window habit.

**Closed out the PRD's remaining live-verification gap.** Owner asked for a broader pass beyond the one
bug-fix regression test, so all four tracks got tested end-to-end against real Supabase + real Anthropic,
not just `reactive_dabbler`:
- `fresh_starter` (`intro → sequencing → complete`) — the compliance-sensitive `sequencing` stage delivered
  real computed numbers (a ₹20,000 surplus, a ₹90,000–1,80,000 buffer range from the user's actual
  outgoings) while explicitly declining to prescribe an order ("None of the three stands in for the other
  two... depends on things specific to you").
- `habit_former` (`intro → gapscan → complete`) — correctly named the missing category (protection)
  generically, never a specific product, using typical figures framed as typical.
- `unclassified`, tested via genuinely ambiguous **free text** (not a chip tap, to exercise the actual
  Haiku classifier rather than the deterministic hint) — correctly stayed unclassified rather than forcing
  a track, then the turn-budget fail-safe fired correctly ("You're good to move on to the rest of the app
  now").

All four tracks, the deterministic chip-hint path, the free-text classifier path, the content-aware
stage-advance classifier, and the turn-budget/`closing_instruction` fail-safe are now confirmed live, not
just reviewed in code. No code changes were needed this pass — every test used the same temporary
dev-bypass/CORS scaffolding as before, reverted immediately after each run, with test rows deleted from
`onboarding_states` afterward.

**What's next:** Nothing blocking. `docs/features/onboarding/PRD.md`'s remaining open items (exact per-stage
copy review, the resume-after-skip interaction) are normal build-time follow-ups, not owner decisions — the
"exhaustive live-testing" item is now closed. The three long-standing owner-only items (D-009 legal review,
D-010 privacy policy, `ANTHROPIC_API_KEY` cloud persistence) remain open. `BRAND_PRD.md`'s three open fields
are the owner's/designer's whenever they're ready to move on the landing page.
