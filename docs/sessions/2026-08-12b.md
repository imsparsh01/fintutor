# Session 2026-08-12b

One bounded fix: the `HealthScoreScreen` back control, flagged but not fixed in the earlier
2026-08-12 session (see that log's "Bugs found outside this task's scope").

## What changed

`app/screens/HealthScoreScreen.tsx` only — `navigation.goBack()` → `navigation.navigate('Portfolio')`
on the "‹ Portfolio" control, with the same explanatory comment ScenarioScreen carries. A bottom-tab
navigator defaults to `backBehavior: 'firstRoute'`, so `goBack()` from a hidden tab screen landed on
Home, contradicting the label.

No decision-log, spec, queue, or codemap changes. The convention was already written into
`docs/CODEMAPS/frontend.md` during BQ-056; this screen was just the one instance that hadn't been
updated to match it.

## Sweep for the same bug elsewhere

Three back controls exist under `app/screens/`:
- `ScenarioScreen.tsx` "‹ Tools" — already fixed in BQ-056.
- `HealthScoreScreen.tsx` "‹ Portfolio" — fixed here.
- `HoldingDetailScreen.tsx` "‹ {family}" — `goBack()` is **correct** and was left alone. That screen is
  the `Detail` route of a family screen's local native stack (`HoldingsStackParamList`), so `goBack()`
  pops to `List`; the tab navigator's `backBehavior` never applies. Its second `goBack()` (post-delete,
  line ~274) is the same stack pop.

`TeachingWalkthrough.tsx`'s `goBack` is a local step-index function, unrelated to navigation.

## Verification

`tsc --noEmit` clean. Ran the `app-dev-user` Expo web preview: Portfolio → Health Score → "‹ Portfolio"
now lands on Portfolio with the Portfolio tab active (previously Home). Metro bundled with no errors.

Environment note: this worktree's `app/` has no `node_modules` of its own. Symlinked the main checkout's
for the typecheck and preview, then removed it — `node_modules/` (trailing slash) in `.gitignore` does
not match a symlink, so it shows up as untracked if left in place.

## Still open (unchanged by this session)

- `app/lib/healthScore.ts` tax utilisation always computes 0 — `'ppf'`/`'epf'` vs the real `'ppf_epf'`
  taxonomy value, and `characteristics.premium_annual` vs the schema's `premium`. BQ-054 defect;
  annualising from the free-text `premium_frequency` needs an owner call (users rely on that figure).
- **BQ-061** — Portfolio overlap indicator, the only unblocked queue item.
- **BQ-058 / BQ-060** — still BLOCKED.
- D-107's gstack gates still cannot run on this machine (`/plan-eng-review`, `/review` absent). This
  change was a two-line, single-file fix, which D-107 exempts as trivial anyway, but the machine-specific
  amendment flagged in the earlier log is still outstanding.
