# 26-Aug-2026 — Account-entry workstream opened; BQ-113 definition package

## What changed

Opened the "Account entry and access" product-definition workstream (portfolio-audit rank 3) after owner
approval, and delivered the BQ-113 definition package.

**Governance**
- Appended **BQ-113..BQ-117** to `docs/BUILD_QUEUE.md` READY (append-only), from the approved FT-002
  proposal; BQ-113 marked WORKING NOW. All five are definition/prototype only.
- Logged **D-152** (owner approved account-entry as next workstream + the BQ-113..117 plan; both HARD-STOPs
  deferred): full write-up `docs/decisions/D-152-account-entry-next-workstream.md`, index line in
  `docs/DECISION_LOG.md`, tracker row `D-152 | READY → BQ-113` in `docs/DECISION_DELIVERY_TRACKER.md`.

**BQ-113 build** — new folder `docs/features/account-entry/`:
- `PRD.md` — user/problem/desired outcome, ties to living-baseline-per-user + privacy-by-design +
  teach-never-advise, success criteria, explicit exclusions, dependencies, evidence ledger.
- `JOURNEY_AND_STATES.md` — discovery→entry→exit journey, alternate paths, and a state matrix covering
  loading, valid, invalid-credentials, expired-session, permission-denied, offline/network-loss,
  account-transition/switch, recovery, not-configured (plus submitting, unconfirmed-email, duplicate-
  registration, logout). Existing screens/navigation reconciled as observed fact with file-path citations.
  Three OPEN owner decisions surfaced (O-A/O-B/O-C), not decided — they are ruled at BQ-115.
- `README.md` — package index matching the arya/baseline pattern.

`app/` and `backend/` were **not** modified. No schema, no new library, no money/legal/principle changes.

## Review gates (D-107/D-145, tool-independent)

- **Pre-build review (scope/edge-cases):** scope = 2 definition docs + governance, fixture-only, no code;
  required state coverage listed and met; three forks must stay OPEN; HARD-STOPs stay DEFERRED; reconcile
  observed screens without modifying them. Recorded before writing.
- **Pre-commit review (correctness/privacy/scope/plan-match):** BQ items match the approved proposal
  objectives/acceptance/size/deps; docs reference D-133/D-137/D-142 without restating; only `docs/` touched
  (`app/`+`backend/` verified clean via git); forks surfaced not decided; HARD-STOPs deferred; proposal's
  stray "D-112 account-switch" citation corrected to the accurate BQ-112 baseline provenance. No substantiated
  findings to fix.

## End-of-session checks

- **D-135 disposition:** D-152 has exactly one tracker row (`READY → BQ-113`). Complete.
- **D-081 archiving:** DECISION_LOG passed 20 with D-152, so D-132..D-137 moved verbatim to
  `docs/DECISION_LOG_ARCHIVE.md`; live log now holds 15 (D-138..D-152). BUILD_QUEUE added READY items only
  (no DONE), PROJECT_SPEC §10 unchanged — no further archiving.
- **Spec/log note:** `docs/DECISION_LOG.md` changed (D-152 added, oldest six archived) — owner should skim.

## What's next

**BQ-114** — Account-entry functional + content + privacy/accessibility contracts (`CONTRACTS.md`): every
state from BQ-113 gets a defined behaviour; auth/ownership references D-137/D-142; error-copy contract
addresses the account-enumeration concern. Then BQ-115 rules the three open forks.

## Integration note

Committed on branch `session/account-entry-bq113`. **Not merged to main and not pushed to main** — per the
task, the orchestrator reviews and integrates (deviation from D-056 auto-merge for this session).

---

# 26-Aug-2026 — BQ-114 account-entry contracts

## What changed

Delivered the BQ-114 contract, building on BQ-113's now-merged (`4d61449`) PRD + journey/states.

**BQ-114 build** — new file `docs/features/account-entry/CONTRACTS.md`:
- Inputs/outputs/persistence: this surface writes no application data of its own — credentials go only to
  Supabase Auth, the "output" is a verified session; no new table/endpoint/schema/library.
- A per-state functional contract row for **every** state in `JOURNEY_AND_STATES.md`'s matrix (not-configured,
  loading, unauthenticated, submitting, register/email-confirm handoff, valid, invalid-credentials,
  duplicate-registration, unconfirmed-email, expired-session, permission-denied, offline, account-switch,
  recovery, logout).
- Enumeration-safe content/copy contract: wrong-password and unknown-email must be **indistinguishable**;
  duplicate-registration must not confirm the email exists; unconfirmed-email is generic. The *constraint* is
  fixed; exact wording is **PENDING O-B**.
- Privacy/security (D-137 JWT ownership, D-142 FastAPI-only tables, D-133 no-provider-leak posture referenced,
  not restated), accessibility, failure and progression (no event; D-121 boundary) contracts.
- O-A/O-B/O-C referenced with clearly-labelled PENDING interim behaviour — **not decided** (ruled at BQ-115).

**Governance**
- Marked BQ-114 done: removed from `docs/BUILD_QUEUE.md` READY and moved verbatim to
  `docs/BUILD_QUEUE_ARCHIVE.md` (D-081), with a short delivery note.
- Updated `docs/features/account-entry/README.md` (CONTRACTS.md → Done) and the D-152 tracker row
  (`READY → BQ-115`).

`app/` and `backend/` were **not** modified. No schema, no new library, no money/legal/principle changes.

## Review gates (D-107/D-145, tool-independent)

- **Pre-build review (scope/edge-cases):** scope = one definition doc + governance, no code; every
  JOURNEY_AND_STATES.md state must get a behaviour; auth/ownership must reference D-137/D-142 without
  restating; error-copy must address account-enumeration; three forks must stay OPEN (PENDING interim). Real
  D-numbers verified in-repo before citing (D-133/D-137/D-142/D-052/D-119/D-149/D-124). Recorded before writing.
- **Pre-commit review (correctness/privacy/scope/BQ-114-acceptance):** (a) every matrix state has a row —
  met; (b) auth/ownership cites D-137/D-142 without restating them — met; (c) enumeration concern explicitly
  addressed with an indistinguishability constraint — met. Only `docs/` touched (`app/`+`backend/` verified
  clean via git). Forks surfaced not decided; HARD-STOPs (test harness, D-095, BQ-092, leaked-password) stay
  DEFERRED. No new open decision surfaced beyond O-A/O-B/O-C. No substantiated findings to fix.

## End-of-session checks

- **D-135 disposition:** no new/changed decision this session (BQ-114 is execution of D-152); D-152 keeps
  exactly one tracker row, updated to `READY → BQ-115`. Complete.
- **D-081 archiving:** BUILD_QUEUE moved a DONE item to archive (correct direction, live DONE count stays 0);
  DECISION_LOG and PROJECT_SPEC §10 untouched — no rolling-window action needed.

## Observed inconsistency (flagged, not fixed — out of BQ-114 scope)

On `main`, BQ-113 still carries the "— WORKING NOW" tag and was never moved to the archive when god merged it
(its session deviated from auto-merge and left integration to the orchestrator). Left as-is; god should
reconcile BQ-113's queue-close during integration.

## What's next

**BQ-115** — `ACCEPTANCE_AND_VALIDATION.md` (5–8 owner task scenarios + recording protocol) and
`DECISION_REGISTER.md` routing O-A/O-B/O-C through the tier protocol; the owner rules the three forks there.

## Integration note (BQ-114)

Committed on branch `session/account-entry-bq114` off `main` (`4d61449`). **Not merged to main and not pushed
to main** — the orchestrator (god) reviews and integrates.

---

# 26-Aug-2026 — BQ-115 account-entry acceptance matrix + owner-ruled UX forks

## What changed

Delivered BQ-115: logged the owner's three now-resolved Tier-2 UX decisions (O-A/O-B/O-C, owner-ruled
26-Aug-2026 taking the orchestrator's recommendations) and built the account-entry acceptance layer.

**Decisions logged (3 new)** — files in `docs/decisions/`, index rows in `docs/DECISION_LOG.md`, tracker rows
in `docs/DECISION_DELIVERY_TRACKER.md`:
- **D-153** (O-A) — session-expiry / network-loss recovery UX = **non-blocking banner + manual retry**; no
  forced logout, no silent re-auth on a transient blip; expired subject treated as lost (no stale data).
- **D-154** (O-B) — duplicate-registration / wrong-password copy = **neutral, enumeration-safe**;
  wrong-password and unknown-email indistinguishable; overrides Supabase's specific default messages;
  ratifies the enumeration-safety constraint already in `CONTRACTS.md` (privacy angle → owner-ruled).
- **D-155** (O-C) — logout / account-switch = **active clear of device-local state** (cached UI/query/
  AsyncStorage), extending the BQ-112/D-149 load-time suppression.
- Each records WHY, is marked owner-ruled, and marked **High reversibility** (definition-stage, no production
  code depends on it yet).

**BQ-115 build** — new file `docs/features/account-entry/ACCEPTANCE_MATRIX.md`:
- Enumerated Given/When/Then acceptance criteria (`AC-*` IDs) for **every** state in
  `JOURNEY_AND_STATES.md` (not-configured, loading, unauthenticated, submitting, valid, invalid-credentials,
  duplicate-registration, unconfirmed-email, expired-session, permission-denied, offline, account-switch,
  recovery, logout, register-success handoff) and **every** journey transition/alternate path.
- Incorporates the three ruled behaviours (D-153/D-154/D-155) — no PENDING/interim behaviour remains — and
  the `CONTRACTS.md` contracts. Cross-references D-137/D-142/D-133/D-119/D-052/D-121 where relevant.
- Cross-cutting invariants (ownership, enumeration-safety, no-leak, no-residue, input-preserved,
  no-progression, a11y), a coverage map proving every state/step maps to ≥1 criterion, and a BQ-116/BQ-117
  mapping (the 5–8 owner task scenarios + recording protocol live in Section E).

**Governance**
- Marked **BQ-115 DONE**: removed from `docs/BUILD_QUEUE.md` READY, moved verbatim to
  `docs/BUILD_QUEUE_ARCHIVE.md` (D-081) with a delivery note recording the filename/format reconciliation.
- Updated `docs/features/account-entry/README.md` (ACCEPTANCE_MATRIX.md → Done; O-A/O-B/O-C → D-153/D-154/
  D-155) and the D-152 tracker row (`READY → BQ-116`).

`app/` and `backend/` were **not** modified. No schema, no new library, no money/legal/principle changes.

## Naming/format reconciliation (flagged for god)

The original BUILD_QUEUE BQ-115 text named `ACCEPTANCE_AND_VALIDATION.md` + `DECISION_REGISTER.md` (routing
the forks through the tier protocol). Per the orchestrator's refined direction, the owner **ruled** the forks
this session, so the "decision register" step became three real decision-log entries (D-153/D-154/D-155)
rather than a routing table, and the acceptance layer was delivered as `ACCEPTANCE_MATRIX.md` (the 5–8 owner
task scenarios + recording protocol are Section E of that file). Substantively equivalent coverage; noted so
integration is not surprised by the filenames.

## Review gates (D-107/D-145, tool-independent)

- **Pre-build review (scope/edge-cases):** scope = 1 acceptance doc + 3 decision files + governance rows, no
  code; every JOURNEY_AND_STATES state/transition must get ≥1 testable criterion; the three ruled behaviours
  must be incorporated with no PENDING left; must cross-reference the new D-numbers + D-137/D-142/D-133; no
  new product scope beyond PRD/JOURNEY/CONTRACTS + the 3 rulings; HARD-STOPs stay DEFERRED. Real D-numbers
  verified in-repo (highest existing = D-152; allocated D-153/D-154/D-155). Recorded before writing.
- **Pre-commit review (correctness/privacy/scope/BQ-115-acceptance):** (a) coverage map confirms every matrix
  state + journey step maps to a criterion — met; (b) each ruled decision has file + log index + tracker row —
  met; (c) enumeration-safety and no-residue expressed as testable invariants — met; (d) only `docs/` touched
  (`app/`+`backend/` verified clean via git). Forks now decided (owner-ruled), not re-surfaced; no new open
  decision introduced; no HARD-STOP resolved. No substantiated findings to fix.

## End-of-session checks

- **D-135 disposition:** D-153, D-154, D-155 each have exactly one tracker row (`READY → BQ-116`); D-152 row
  updated to `READY → BQ-116`. Complete.
- **D-081 archiving:** DECISION_LOG went 15 → 18 with the three new entries (≤ ~20, no archiving needed).
  BUILD_QUEUE moved a DONE item (BQ-115) to archive (live DONE count stays 0). PROJECT_SPEC §10 untouched.
- **Spec/log note:** `docs/DECISION_LOG.md` changed (D-153/D-154/D-155 added) — owner should skim.

## What's next

**BQ-116** — Account-entry interactive fixture prototype (standalone HTML/CSS/JS, fixture-only), exercising
every acceptance criterion including expired-session (banner + manual retry), offline (banner + manual retry),
wrong password, duplicate registration, logout (active clear), account switch (no residue), and
not-configured. Then **BQ-117** owner validation walkthrough + disposition.

## Integration note (BQ-115)

Committed on branch `session/account-entry-bq115` off `main`. **Not merged to main and not pushed** — the
orchestrator (god) reviews the diff and integrates.

## Housekeeping (BQ-115 integration)
- D-081: relocated the completed BQ-113 entry from `BUILD_QUEUE.md` READY to `BUILD_QUEUE_ARCHIVE.md` verbatim (it had been left inline marked DONE across two prior sessions). No content change.
