# Session 2026-08-03j

**What:** Owner asked for my read on BRIEF-007, then confirmed it. Unlike BQ-004, I recommended
**Path A** (not deferring): WHICH is buildable today with no interface dependency, and deferring
would reverse D-012's explicit "core MVP, all types" commitment rather than merely postpone a stub.
Logged as **D-051** — Path A adopted, staged: WHICH built now, WHEN stays gated until D-032's
on-topic constraint is re-verified via Phase-1 fixture testing in this new scenario.

Scoped the WHICH build as **BQ-013** and proposed a v1 pairing table before writing any code (real
product-judgment content, flagged by BRIEF-007 itself as deserving scrutiny). One open sub-question
put to the owner directly: does holding Endowment/ULIP suppress the term_insurance candidate? Owner
chose **don't suppress** — D-013 already split Term from Endowment/ULIP because the teaching moment
differs, so the gap is real regardless.

Built `backend/app/services/surfacing.py`'s `compute_surfacing_candidates()` (fixed pairing-rule
table, Holdings-only, zero model judgment) and `GET /surfacing-candidates?user_id=`. Verified
end-to-end against the live Supabase DB across four scenarios (loan+no-insurance, loan+Endowment/
ULIP-no-Term, loan+Term-already-held, no-loans) — all matched expectations, including the
non-suppression case. Test rows deleted after verification. BQ-013 moved to DONE.

**What's next:** WHEN (the on-topic re-verification) is still gated — nothing built this session
surfaces anything to a live user. Build queue's READY list is empty again.
