# Session 2026-08-04g

**What:** Fresh session, picked up from 2026-08-04f's resume-from-here summary. Owner asked to keep
working autonomously through the READY queue without stopping for confirmation on bounded, non-decision
items — checking in only where a real dependency (a genuine decision, ambiguity, or hard-stop) surfaces.
Nine build items shipped this session, one Tier-3 decision logged along the way.

**BQ-016 — Income CRUD API.** `backend/app/services/income.py` + `GET`/`POST /income`, `PUT
/income/{id}` (full replace of `sources`). Traces to D-038.

**BQ-017 — Goals CRUD API.** `backend/app/services/goals.py` + `GET`/`POST /goals`. `progress` computed
live as the sum of `funded_by` earmarked amounts, per D-038's own text — never stored on the `Goal` row.

**D-065 + BQ-018 — Consolidated net-worth aggregation, escalated mid-build.** Unlike BQ-010/BQ-017, no
formula was already decided for this one — FD/RD has no accrued-value field, and `PROJECT_SPEC.md` didn't
say signed-net vs. per-family totals. Stopped and asked rather than guessing (`docs/decisions/
D-065-consolidated-aggregation-shape.md`). Owner resolved: FD/RD uses `principal_or_monthly_amount`
as-is (no accrual formula invented), endpoint returns separate `investments_total`/`loans_total`/
`insurance_total`, not one net figure. ESOP excluded (D-055 left its characteristics schema undesigned).

**BQ-029 — Engagement streak model + API.** `StreakState` model (hand-written Alembic migration — no
`DATABASE_URL` in this sandbox to autogenerate against, matched column-for-column to existing migrations'
conventions), `GET`/`POST /streak/open`. Increment/reset/no-op logic unit-tested against a mocked session.

**BQ-030 — Variable reward trigger.** `evaluate_reward()` — a variable-ratio probability roll gated to one
attempt per genuinely new streak day, wired into `POST /streak/open`.

**BQ-031 — Streak + reward UI.** `StreakBadge` + `Mascot` mood reacting to `reward_fired`, wired into
`ConsolidatedScreen`. Haptic feedback scoped out — would need a new dependency (`expo-haptics`) not
decided here.

**BQ-021 — Consolidated screen wired to real aggregation.** `ConsolidatedTotalsCard`, replacing the
placeholder body text with real per-family totals.

**BQ-023 — Core teaching/chat backend endpoint. The biggest item in the queue.** `backend/app/services/
baseline.py` assembles the exact JSON profile slice `SYSTEM_PROMPT_v0_8_runnable.md` §4 documents —
holdings carry **alias + characteristics only, `display_name` never included**, confirmed by a unit test
that a real name never appears anywhere in the assembled JSON. `deepen` omitted per D-028's decided
default. One real gap surfaced and left open, not patched: `baseline.dependents`/`emergency_fund_months`
have no backing schema field — omitted rather than guessed (adding them would be a schema decision).
`backend/app/services/teaching.py` calls the Anthropic API with the exact message shape
`scripts/run_phase1_test.py` already validated across Phase 1 runs (model `claude-sonnet-5`,
`max_tokens=4096`). `POST /chat` added. No live API call was possible (this sandbox blocks
`api.anthropic.com` by design) — verified via unit tests with the Anthropic client mocked.

**BQ-024 — Chat/conversational UI screen.** `ChatScreen` + a new `Chat` tab in `MainTabs`. Message history
is local display state only — the model never receives prior turns (D-022). Tab placement is a judgment
call, not a decided spec item — flagged for BQ-025 to likely replace once D-058's onboarding shape is
built.

**Every item verified** the same way prior sessions established: backend — `python -m py_compile`, fresh-
venv import + route registration, no-DB 500 smoke test, plus targeted unit tests (mocked DB sessions,
mocked Anthropic client) for anything with real logic (streak increment/reset, reward probability,
baseline assembly, budget-adjacent math). Frontend — `npx tsc --noEmit` + `npx expo export --platform
android`. Nothing was verified end-to-end against a live database or live Anthropic API — consistent with
every prior session's documented limitation in this remote sandbox.

**One decision logged (D-065)** — see above. `PROJECT_SPEC.md` unchanged. `docs/BUILD_QUEUE.md` updated
after every item (moved to DONE, downstream items' BLOCKED status refreshed).

**Update, same session — five more items shipped, queue driven to empty of anything build-time-executable.**
Owner confirmed continuing autonomously; check-ins happened only where a real dependency surfaced.

**BQ-025 — Onboarding flow (D-058).** Extracted `ChatThread` out of `ChatScreen` (shared message-thread UI,
imperative `send()` via ref) and added `OnboardingScreen`: four chip starters, no structured field, "Skip
for now" always visible. Wired into `RootNavigator` as the default landing screen after auth, never a hard
gate, dismissal persisted locally via AsyncStorage (`lib/onboarding.ts`). The `Chat` tab is kept as the
resume surface — corrects BQ-024's own speculation that onboarding would replace it; in practice the two
turned out complementary.

**BQ-020 — Budgeting/Goals tab.** `lib/budget.ts`/`income.ts`/`goals.ts` + `BudgetingScreen`: live budget
summary, income sources with an inline add form, goals with live `progress` and an inline add form (no
`funded_by` picker yet — goals created unfunded, scoped out same as BQ-027 scoped characteristics
editing). Extracted `formatRupees` into `lib/format.ts`, shared with `ConsolidatedTotalsCard`.

**BQ-028 — Holdings characteristics editing UI.** `lib/characteristicsSchema.ts` (D-013's per-type field
lists, `esop` originally excluded — D-055 left its schema undesigned) wired into a new dynamic section in
`HoldingEditModal`: enum fields as chip toggles, everything else as text/numeric input, resets on
recategorize.

**BQ-022 — Holding-detail view.** Real navigation-shape call, reasoned through rather than escalated: each
family tab became its own small stack (`List` → `Detail`), row tap now goes to a new read-only
`HoldingDetailScreen` with an "Edit" button opening the same `HoldingEditModal` one tap deeper (BQ-027's
full authority preserved) and an "Ask about this" button cross-navigating to `Chat` with a pre-filled
question. **Caught a real compliance-adjacent bug during design, not after:** the pre-fill had to use the
holding's alias only, never `display_name` — `/chat`'s question text is sent to the LLM verbatim, so an
app-generated message carrying a real name would have been a genuine D-010 violation, distinct from the
already-handled case of a user typing a real name themselves.

**D-066 — ESOP characteristics field schema.** Applied D-013's split-vs-merge test directly (Tier 2,
REVIEW-FLAGGED, acted on immediately per the protocol's design): ESOP stays one type per D-055's scope,
`grant_type` (`options`/`rsu`) distinguishes internally, eight fields land in `characteristicsSchema.ts`,
closing the gap BQ-028 shipped with. No stored `vested_units` — derivable, not designed here.

**`PROJECT_SPEC.md` changed this session (§6, v3.4)** — owner explicitly confirmed updating the ESOP note
from "not yet designed" to resolved, reflecting D-066, per §8's own propose-then-confirm edit rule.

**Queue now empty of anything executable without the owner:** BQ-026 (comparison-view modal) needs both a
detection mechanism and real breakeven/tax-modeling formulas, neither decided anywhere — flagged in
`BUILD_QUEUE.md` itself for whoever picks it up next. BQ-004 (`deepen` selection rule) now has the
interface it needed (BQ-023/024) but the selection rule itself is still an open regulatory question
(BRIEF-006). Variable-income budgeting (BRIEF-011) remains parked pending the owner's decision.

**Session total: 14 BQ items + 2 decisions (D-065, D-066), one `PROJECT_SPEC.md` edit.** Every item
verified per this session's established pattern (backend: `python -m py_compile`, fresh-venv import + route
registration, no-DB 500 smoke test, targeted unit tests for real logic; frontend: `npx tsc --noEmit` + `npx
expo export --platform android`) — nothing verified end-to-end against a live database or live Anthropic
API, consistent with this remote sandbox's documented limitations throughout.

**What's next:** pick one to unblock the queue — talk through BQ-026's detection mechanism and comparison
math, settle BQ-004's selection-rule question, or decide the variable-income budgeting approach. No purely
mechanical build-queue work remains.
