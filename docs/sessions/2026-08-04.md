# Session 2026-08-04

**What:** Owner asked what's left before scaffolding the app; split it into "bare skeleton"
(nearly unblocked — stack already decided, no design dependency) vs. "real screens" (blocked on
D-012's undesigned onboarding/capture pieces). Owner confirmed proceeding with the bare skeleton.

One real fork needed a call first, same shape as D-041: React Navigation (manual) vs. Expo Router.
I recommended Expo Router; owner chose React Navigation for explicit routing control. Logged as
**D-052**, queued as **BQ-014**.

**Environment blocker hit mid-build:** Node.js wasn't installed on this machine at all. Installing
system-level software is outside D-034's repo-scoped autonomy grant, so asked first — owner
confirmed, installed Node LTS via Homebrew.

**Built BQ-014** — first code ever in `app/`: Expo + TypeScript, React Navigation (auth stack +
bottom-tab shell for Investments/Loans/Insurance/Consolidated per D-031), Supabase JS client that
degrades to a clear "not configured" screen rather than crashing (mirrors the backend's
missing-`DATABASE_URL` pattern), a `/health` ping surfaced on the Consolidated screen. Immediately
stripped `create-expo-app`'s auto-generated `AGENTS.md`/`CLAUDE.md`/`.claude/`/`LICENSE` boilerplate
— a second, different operating-rules pair nested in `app/` would have recreated exactly the drift
risk D-045 eliminated this session. Also fixed: root `.gitignore`'s `.env.*` rule was unintentionally
catching `app/.env.example`; added a negation.

Verified `tsc --noEmit` clean and a clean Metro bundle (1069 modules). **Could not get visual
simulator confirmation** — the iOS Simulator panel tool crash-looped and its own error said
retrying would not help. Flagged rather than claimed as done; headless checks are solid evidence but
not a substitute for actually seeing it render.

**Update, same session:** owner asked for a step-by-step guide to the Supabase credentials, then
opened the Supabase dashboard in the Browser pane. Walked through it with them there — landed on
Settings → API Keys, found the Publishable key (Supabase's current term for the client-safe key,
explicitly labeled safe to share publicly) and the project URL via Data API → API URL. Owner
confirmed writing both into `app/.env`. Re-verified headlessly: dev server log confirms the env vars
loaded, and both values are correctly inlined in the served JS bundle — `isSupabaseConfigured` will
now evaluate `true`. D-052's dependency flag is resolved.

**What's next:** the iOS Simulator panel is still crash-looping (tried twice, its own error says
retrying won't help) — visual/interactive confirmation (seeing the login screen, testing
register/login against the real Supabase project) is still pending the owner reopening it. Real
screens (onboarding, capture, per-item management) stay blocked on D-012's undesigned pieces, now
genuinely unblockable in principle since a real interface exists to design against.
