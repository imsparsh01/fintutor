# Session 2026-08-04b

**What:** Session opened with a sync check (already up to date with `main`, no action needed) and an
explicit owner instruction: sync the working branch to `main` after every sizable chunk this session, not
just at the end — followed throughout (every item below was committed, pushed, and fast-forward-merged
into `main` individually).

**Founding UX/UI strategy request, scoped before acting.** Owner asked for a `UX_GUARDRAILS.md`, a UX
folder structure, backlog user stories, and next steps, based on a 25-principle framework drawn from
Duolingo/Finch/Brilliant/Mimo/Fabulous/Habitica. Flagged two real blockers before touching anything: (1)
`PRODUCT_PRINCIPLES.md`'s "deliberately unprincipled" note explicitly sequences a UX section after
Decision 2 AND Decision 3 both exist — Decision 2 (`PROJECT_SPEC.md` §8, per-item management depth) was
still open; (2) gamification mechanics (streaks, loss aversion, dopamine bursts) genuinely tension with
"teach never advise"/"start strict" and needed live discussion, not a unilateral pick. Owner chose:
resolve Decision 2 first, discuss the philosophy live.

**Decision 2 resolved as Path C (D-059).** Checked readiness first (found `PROJECT_SPEC.md`'s own
"Phase-1-section" dependency partially met — real screens existed as placeholders, but not wired to the
real Holdings API) — owner chose to wire them first (**BQ-019**: `app/lib/holdings.ts`, `taxonomy.ts`,
`AuthContext.tsx`, `HoldingsList.tsx`; verified `tsc` clean + Metro bundle). Then presented Decision 2 as
a proper Tier-3 brief (three paths: view-only / delete-only / full CRUD) — owner chose full per-field
edit, delete, and recategorize authority, explicitly aware of the tension with the "living baseline, not a
CRUD save" framing. Logged as **D-059**, `PROJECT_SPEC.md` §8/§10 updated (v3.1) after owner confirmed the
exact edit.

**Owner asked which path was faster — built, not discussed.** Chose implementing Decision 2 over the UX
discussion for speed. Shipped **BQ-027** (backend `PATCH`/`DELETE /holdings/{id}`, frontend
`HoldingEditModal.tsx` — tap-to-edit, chip-based recategorize picker, destructive delete behind a confirm)
and logged the honest gap it left (`characteristics` field-level editing not in the UI yet) as **BQ-028**,
deliberately deferred at the owner's request rather than built now.

**UX/gamification philosophy, discussed live, cluster by cluster.** Sorted all 25 candidate principles
into clean fits, outright-blocked (contradicts D-058/§5, no decision needed), and two genuinely contested
clusters:
- **Cluster 1 — engagement mechanics (streaks, variable reward, Hook Loop).** Owner chose full adoption,
  "same playbook as Duolingo," explicitly aware of the gambling/social-media mechanic-shape. Logged as
  **D-060** — Tier 3, fires trigger 3 (runs counter to P4's start-strict default, recorded as a scoped
  carve-out, not a supersession) and trigger 5 (MVP scope increase, deliberately not applied to the spec
  yet, pending cluster 2).
- **Cluster 2 — real financial data as a game (XP/badges, virtual pet, RPG skin).** Owner chose the
  middle ground: cosmetic, behavior-reactive elements permitted; nothing may react to the user's real
  financial data. Logged as **D-061** — Tier 3, **interprets P6** into gamification design for the first
  time (new operative test: does the game element react to app behavior, or to real money?).

Applied the batched **`PROJECT_SPEC.md` §4 scope edit** (v3.2, new item 9) once both clusters landed, and
wrote up **P7** in `PRODUCT_PRINCIPLES.md` (v1.2) — the actual "guardrails" deliverable from the original
ask, done as real deliberation with tension stated on the record rather than a unilateral 6-of-25 pick.
"Deliberately unprincipled" note updated honestly: gamification now has a principle; the broader UX/
interaction-model section D-031 anticipated is unblocked (both Decision 2 and 3 now resolved) but still
unwritten, its own future task.

**Backlog + folder structure (the remaining two original asks).** Translated P7/D-060/D-061 into
`BUILD_QUEUE.md` items: **BQ-029** (streak state model + API, unblocked), **BQ-030** (variable reward
trigger logic, depends on 029), **BQ-031** (streak/reward UI, depends on 029+030). Created **`docs/ux/`**
(journeys/ and wireframes/, each with a convention README + one real worked example — `onboarding.md`
from D-058, `holdings-list.md` from the shipped `HoldingsList`) and **`app/design/tokens.ts`** (seeded
from colors/spacing already scattered across real screens, not invented) — deliberately not the generic
`company/product/ux/` template the original prompt suggested, matched to this repo's actual flat-monorepo
convention instead.

**BQ-032 (mascot) finished.** The one open piece needing an actual creative call, not engineering — asked
the owner directly (three concepts, each pre-checked against P7 and flagged where relevant: an owl was
avoided since Duolingo already uses one). Owner chose **Ankur**, a sprout/plant character. Built
`app/components/Mascot.tsx` (mood states, placeholder emoji, uses the new design tokens), given a home on
`ConsolidatedScreen`. Not wired to live triggers yet — the streak/reward and chat surfaces it would react
to (BQ-029–031, BQ-023/024) aren't built.

**Decisions made this session:** D-059, D-060, D-061 (all Tier 3, all owner-decided directly, all logged
with the tension/interpretation made explicit rather than smoothed over). Plus P7 added to
`PRODUCT_PRINCIPLES.md`.

**Build items shipped this session:** BQ-019, BQ-027, BQ-032 — all verified via `tsc --noEmit` clean and a
clean Metro/`expo export` bundle; none verified against live data (no `DATABASE_URL` in this remote
session, same limitation prior sessions hit).

**What's next:**
1. **`BQ-028`** — Holdings `characteristics` field-level editing UI. Ready whenever picked up; no new
   decision needed.
2. **`BQ-029` → `BQ-030` → `BQ-031`** — the streak/reward engine, in that dependency order. `BQ-029` is
   fully unblocked right now.
3. **`BQ-032`'s live wiring** — Ankur's mood only changes once BQ-029–031 (streaks/rewards) and
   BQ-023/024 (chat surface, still blocked) exist to react to.
4. **The broader UX/interaction-model principles section** D-031 anticipated (persistent sections,
   AI-primary/manual-secondary capture, aliases never shown, progressive capture) — genuinely unblocked
   now (Decision 2 and 3 both resolved), still unwritten. Worth its own session, not squeezed into this
   one.
5. **Everything already pending from before this session** (variable-income budgeting for the startup/gig
   profile, ESOP's characteristics schema, `PROJECT_SPEC.md` §3's "not defined by age" open framing
   question, the comparison-view modal shape, BQ-023/024 — the core teaching/chat engine) is unchanged —
   not touched this session. See `docs/sessions/2026-08-03k.md`'s closing summary for the full standing
   list rather than repeating it here.

**Sync note:** every item above was individually committed, pushed to `claude/sync-main-branch-ao9xpf`,
and fast-forward-merged into `main` as it landed (D-056) — a fresh session pulling `origin/main` sees all
of it, nothing is stranded on the working branch only.
