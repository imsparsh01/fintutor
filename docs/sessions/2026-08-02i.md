# Session 2026-08-02i

**Task:** Run and score BQ-008 — verify D-037's fix for FINDING 9 (Card-1 omission).

## What changed
- Ran BQ-008 (n=5, Q1 vs SYSTEM_PROMPT_v0_8_runnable.md, FIXTURE_user_01.json) via the owner's local
  `scripts/run_phase1_test.py`.
- First attempt: 3/5 valid, 2/5 empty with no error. Diagnosed by adding raw-response capture to the
  script — found `stop_reason=max_tokens` with `thinking_tokens` consuming the entire 1024-token cap, so
  two calls spent their whole budget on internal reasoning and never produced visible text. This was a
  test-tooling bug (max_tokens too low for a thinking-enabled model), not a prompt or compliance issue.
  Fixed `scripts/run_phase1_test.py`'s default `max_tokens` from 1024 → 4096.
- Re-run came back clean, 5/5. Scored against FINDING 9, FINDING 10, and FINDING 11 (per D-036's
  clarified test). Results: **FINDING 9 fixed (5/5)**, **FINDING 10 still fixed (0/5)**, **FINDING 11
  clean (0/5 violations)**, all runs within the 320-word ceiling.
- Wrote `docs/PHASE1_RUN6_RESULTS.md` with full scoring detail.
- Moved BQ-008 from READY to DONE in `docs/BUILD_QUEUE.md`.
- Updated `PROJECT_SPEC.md` §8 (FINDING 9 entry now marked VERIFIED, not pending) and change log (v2.7).

## Why
D-037 was a Tier 2, REVIEW-FLAGGED decision — it took effect immediately but needed empirical
verification before counting as settled, same discipline applied to every prior fix this project. BQ-008
was that verification step.

## What's next
- No open build task queued right now (BUILD_QUEUE.md's READY section is empty). Standing open items
  from the "pending on owner" list are unchanged: Decision 3 (budgeting/goals data model), BQ-004
  (blocked on that), the D-013 idle-cash taxonomy question, legal review of D-009, data privacy policy
  (D-010), and periodic GitHub token / Anthropic API key rotation.
- D-037 remains REVIEW-FLAGGED — the owner can still retroactively veto it per Tier 2's design, though the
  evidence now supports it (5/5 clean vs. the earlier 3-misses-across-10-runs base rate).
