# Session — 31 Aug 2026

## D-175 / BQ-149 owner PASS and close

- Owner recorded **PASS** for the complete Calculator definition, D-174 formula/safety package, controlled prototype and exhaustive QA.
- D-175 freezes the validated package at prototype commit `4f83dfb`. Prototype evidence remains definition evidence and cannot substitute for production parity.
- BQ-149 moved verbatim to the archive. BQ-150 is the sole READY item for a production-reconciliation plan; D-173/D-175 point to it and D-174 remains SHIPPED to BQ-148 evidence.
- No production code, formula, schema, route, dependency, persistence, tax/HRA boundary or catalogue scope changed.
- Next: execute BQ-150 as one bounded planning session, then implement only its separately queued production items.

## BQ-150 completion — Calculator production-reconciliation plan

- Audited the frozen D-174/D-175 Calculator package against the production screen, Emergency component, pure engines/tests, progression and shared Scenario handoff.
- Added `PRODUCTION_RECONCILIATION.md`: every gap maps to an approved outcome. Work is separated into BQ-151 exact engines/domains, BQ-152 lifecycle/evidence/candidate boundary, BQ-153 Arya/progression and BQ-154 exhaustive production parity.
- Key findings: four older embedded engines plus Step-up need strict pure-domain reconciliation; lifecycle/result evidence is inconsistent; Card's recorded-candidate path contradicts the approved manual-only scope; all Calculator handoffs are missing. Existing newer engines, Emergency mechanism and backend progression rules are retained.
- No new decision, backend route, schema, dependency or persistence need was found. No production code changed. Tax/HRA remains deferred and the exposed local legacy service-role key will not be used.
- Documentation review and `git diff --check` PASS. BQ-150 moved verbatim to the archive; BQ-151 is the sole READY item and D-173/D-175 point to it.

## BQ-151 completion — exact Calculator engines and numeric domains

- **Pre-build lock:** only SIP Goal, Home Loan EMI, Inflation Impact, Step-up SIP and CAGR formula/domain extraction plus strict raw parsing. No UI redesign, candidates, Arya, progression, backend, schema, dependency or persistence work.
- Added pure `calculatorEngines.ts` for SIP/EMI/Inflation/CAGR with D-174 amount/rate/period ceilings, zero-rate branches, fractional horizons where approved, signed CAGR, stable `log1p`/`expm1` arithmetic and finite/`MAX_SAFE_INTEGER` output guards. Reconciled Step-up SIP to explicit 0% return/step, 1..200 whole years, 0..1,000% rates, ₹1 lakh crore input and safe iterative outputs.
- Production screen now uses the shared strict whole-string parser and pure engines for the five targeted tools; permissive `parseFloat`/`parseInt` no longer drives their calculations. No result presentation/lifecycle change was absorbed.
- Verification: 123/123 frontend tests PASS including new normal/zero/equality/loss/fraction/exact-edge/just-outside fixtures; TypeScript PASS; production web export PASS; `git diff --check` PASS.
- Pre-commit review: correctness PASS; security/privacy PASS (pure local arithmetic); scope PASS. Frontend codemap updated. Existing `.DS_Store` changes untouched.
- BQ-151 moved verbatim to archive. BQ-152 is sole READY; D-173/D-175 point to it.

## BQ-152 completion — Calculator lifecycle, evidence and candidate boundary

- **Pre-build lock:** only the approved nine-Calculator UI lifecycle, result evidence and candidate boundary. No formula, Arya, progression rule, backend, schema, dependency or persistence change.
- All eight manual screens now use strict whole-string parsing, typed errors, immediate current-result invalidation with the frozen neutral notice, reset/clean reopen and native-safe result focus. Every valid result freezes inputs/authorship, formula/convention and rounding/caps/omissions.
- Credit-card Payoff no longer fetches or selects recorded holdings. Emergency recorded FD and budget values load independently as source-labelled, excluded-by-default candidates; only explicit Include copies one into the editable draft. Reset excludes them again, account generation clears prior state, and the division formula is unchanged.
- Verification: 127/127 frontend tests PASS, including four new production lifecycle contract checks; TypeScript PASS; production web export PASS; `git diff --check` PASS. Rendered web smoke reached the authenticated login boundary with no runtime errors; the existing Expo Notifications web-support warning remains unrelated. Authenticated all-state rendering is reserved for BQ-154.
- Pre-commit review: correctness PASS after preventing an empty holdings response from appearing as a recorded zero candidate; security/privacy PASS (Card is manual-only, Emergency ownership boundary unchanged, no persistence); scope PASS; accessibility PASS for labelled fields, ≥44px actions, alert/live result and native-only focus guard. No substantiated finding remains.
- Frontend codemap updated. BQ-152 moved verbatim to archive; BQ-153 is sole READY and D-173/D-175 point to it. Existing `.DS_Store` changes remain untouched.

## BQ-153 completion — Calculator Arya and progression integration

- **Pre-build lock:** only current-result Calculator handoffs and progression contract proof, reusing the existing Scenario handoff builder/modal and existing progression emitter. No formula, model call, backend, schema, dependency or persistence change.
- All nine valid current Calculator results now expose “Explore the mechanism with Arya.” The confirmation modal shows the exact payload and sends nothing on Cancel; Confirm alone navigates to Chat with Calculator type, normalized finite inputs, formula boundary and omissions. Names, aliases, institutions, record/source fields and user IDs remain rejected by the shared builder.
- Extended the shared handoff builder with a Calculator label while preserving Scenario output by default. Emergency sends only normalized included totals, never candidate record metadata.
- Progression remains one stable type-only `calculator_completed` call after valid result render. Invalid, changed, capped, reset, modal-open/cancel/confirm and handoff paths contain no emitter; backend cap/failure stays fire-and-forget and cannot change UI.
- Verification: 131/131 frontend tests PASS, including shared builder and three Calculator integration contract checks; TypeScript PASS; production web export PASS; `git diff --check` PASS. Authenticated modal/state rendering remains part of BQ-154's exhaustive configured parity gate.
- Pre-commit review: correctness PASS; security/privacy PASS; scope PASS; test evidence PASS. No substantiated finding remains. Frontend codemap updated. BQ-153 moved verbatim to archive; BQ-154 is sole READY and D-173/D-175 point to it. Existing `.DS_Store` changes remain untouched.

## BQ-154 in progress — exhaustive production Calculator parity

- **Pre-build lock:** production evidence and fixes required to make all 55 acceptance IDs and 51 canonical states true; no prototype substitution, new formula, backend route/schema, dependency, persistence or catalogue scope.
- Baseline verification: exactly 55 unique AC IDs and 51 canonical states; 131/131 frontend tests, TypeScript and web export PASS; complete backend suite 401/401 PASS.
- Audit finding fixed: Emergency Coverage still derived aggregate candidates from raw Holdings/Budget clients, which could not meet the approved component-level provenance, malformed/empty and typed failure contract. It now reuses the authenticated `/scenario-candidates` component-evidence API, displays source label/field/version/freshness/retrieval/value status, keeps candidates excluded, distinguishes permission loss from retryable failure, clears on permission loss and exposes explicit retry.
- Added pure candidate-offer fixtures proving empty is not recorded zero, available components sum without losing evidence, and malformed/unavailable values never enter arithmetic. Responsive content is now bounded at 720px. Current verification: 134/134 frontend tests and TypeScript PASS.
- BQ-154 remains READY/in progress. Next: complete the explicit 55-AC/51-state production ledger, resolve any remaining accessibility/responsive findings, run configured production/browser evidence, and request owner disposition. Existing `.DS_Store` changes remain untouched.

### BQ-154 parity checkpoint 2

- Added a production-only evidence ledger with exactly one explicit row for every 55 AC IDs and 51 canonical states. Machine integrity tests compare its sets to both binding source matrices and prohibit prototype substitution. `PENDING-LIVE` rows remain visibly open rather than being inferred from passing automation.
- Audit fixes: every Calculator focus-session now remounts clean on reopen/account/type change; all nine expose an explicit accessible return to Tools; all manual tools show shared assumption-authorship scope; Emergency shows the same no-write/no-adequacy scope.
- Malformed raw input now focuses the first invalid field. Domain/cross-field failures focus the relevant first field, and summary errors are included in that field's accessible label. Emergency implements the same behavior with typed field association.
- Result headings now receive guarded web/native focus after one announcement. Canceling the exact-payload Arya modal restores the precise opener on web/native; modal confirm remains the only navigation/send path. Calculator content is bounded to a 720px reading width.
- Emergency candidates remain visible as Included/Excluded checkboxes, preserve component-level provenance in frozen results, support explicit exclude and explicit refresh, and preserve manual edits while refresh removes stale recorded authorship/result eligibility.
- Verification after these fixes: 137/137 frontend tests PASS; TypeScript PASS; production web export PASS; complete backend suite remains 401/401 PASS; `git diff --check` PASS.
- Remaining binding work: configured authenticated rendered checks for exact payload/cancel, offline/reconnect, result/modal focus, keyboard/Escape, 320/390/1440/200%, themes/high contrast/reduced motion and console/DOM; then owner disposition. BQ-154 remains open.

### BQ-154 configured-stack checkpoint

- Configured FastAPI `/health` and Supabase `/health/db` both returned PASS. The authenticated Scenario-candidate route returned the correct HTTP 401 `Authentication required` response without a bearer token.
- The production web app is running in the visible in-app browser but remains at Login; no credential was entered or inferred. Authenticated Calculator observations remain pending rather than being fabricated from the login shell.
