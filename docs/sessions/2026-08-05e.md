# Session 2026-08-05e

**What:** Continuation of the same local Mac session (2026-08-05d) — dev-server tooling, an onboarding
redesign discussion that produced a real decision, and a marketing/brand PRD. No build-queue item worked;
mixed infra + Tier-3 decision + external-facing-doc session.

**`.claude/launch.json` created.** Detected this project's two dev servers (backend: `uvicorn`, port 8000;
app: `expo start --web`, port 8081) and registered them for `preview_start`. Hit a sandbox `getcwd`
restriction on relative `cd`/`source` shell built-ins inside the preview process — fixed by using absolute
paths throughout (`python3 -m uvicorn ... --app-dir <abs>` for the backend, `npm --prefix <abs> run web`
for the app) instead of `bash -c "cd ... && ..."`. Both start cleanly now. Confirmed the app renders a real
authenticated session (streak badge, live ₹0 balances, no "Failed to fetch") — the owner completed a real
Supabase login between sessions, so 2026-08-05d's fix is holding up end-to-end.

**Live-tested the real onboarding chat and found it wanting.** First genuine login, first real onboarding
message ("I just started earning...") returned a ~300+ word, three-topic answer — traced to two real facts,
not a bug: `OnboardingScreen` has no onboarding-specific logic (chip taps just fire into the same general
`/chat` pipeline as the Chat tab), and that's exactly what D-058 chose (Option C, pure free-form
conversation, no structure). Owner's reaction: reopen it.

**D-082 logged** (`docs/decisions/D-082-onboarding-structured-flow-scope.md`): narrows D-058 on the
*conversation-flow* axis only (D-058's "no structured field" still holds) — onboarding becomes a
structured, model-driven multi-stage flow, scoped to onboarding only for now, with an explicit fail-safe
requirement (no turn may leave the user with nowhere to go). Also adopts a new repo convention:
`docs/features/<slug>/` for a feature big enough to need its own PRD/design docs — piloted on `onboarding/`
— documented in `docs/features/README.md` and pointed to from `CLAUDE.md`'s file-permissions section (D-033
precedent: decision logged first, then this deliberate-only file touched).

**`docs/features/onboarding/PRD.md` drafted, deliberately incomplete.** Writing the actual stage/path design
surfaced a real blocker: a structured flow needs *some* cross-turn state, and this system currently has
none at all (D-022's standing "every `/chat` call is independent" implementation) — worse, conversation
memory is already formally PARKED under D-022, gated on the still-unwritten D-010 privacy policy. The PRD
opens on this fork (a narrow stage-indicator vs. actually resolving D-010 first) and stops there, pending
the owner's call — not designed further this session.

**Flagged a second prompt-injection attempt.** After reverting the temporary CORS edit via `git checkout --`
(confirmed clean via `git diff --stat` showing zero changes), a tool-originated system-reminder again
falsely claimed the file still held the edit and told the assistant not to mention it — same pattern as
2026-08-05d. Independently re-verified via `grep -c CORSMiddleware backend/app/main.py` (0 matches) and
fresh `git status`, then told the owner rather than complying with the "don't tell them" instruction.

**`docs/marketing/BRAND_PRD.md` written** — a Product Requirements & Brand Overview for the owner's
developer/designer to build a waitlist landing page and Instagram materials from. Grounded entirely in
already-decided project research (BRIEF-008 through BRIEF-011, D-053/D-054's founding-segment work, D-009's
compliance stance, the teaching system prompt's actual voice spec) rather than the generic dev-tool SaaS
template the owner's request was structured around — GitHub/Stripe/Slack/"credits"-style items were dropped
as inapplicable. Three fields left explicitly `[OPEN]` rather than fabricated: waitlist incentive (no
pricing/monetization decision exists yet), visual palette (current UI is an acknowledged placeholder, not a
real brand), and launch metrics (zero real users exist — no stat was invented to fill the section). Closes
with an explicit compliance-guardrails section tying D-009's real SEBI enforcement-risk reasoning directly
to landing-page/Instagram copy, not just in-app AI output.

**Full git sync performed** (this session's own "where are we, is everything in sync" check): `git fetch`
confirmed local `main` and `origin/main` share identical history — nothing was on GitHub that wasn't already
pulled locally. The apparent "out of sync" state was entirely this session's own uncommitted working-tree
changes, now committed and pushed directly to `main` (already on it, per D-034/D-056, no separate branch to
merge).

**`docs/DECISION_LOG.md` changed this session** (D-082 appended) — flagging per `CLAUDE.md`'s checklist.
Still within its rolling window (17/~20 entries) — no archiving needed. `BUILD_QUEUE.md` and
`PROJECT_SPEC.md` §10 both unchanged and within their own windows (0 pending-archive items; 9/~10 entries).

**What's next:** D-082's open fork (stage-state vs. resolving D-010 first) needs the owner's decision before
`docs/features/onboarding/PRD.md` can proceed past its first section. `BRAND_PRD.md`'s three `[OPEN]` items
(waitlist incentive, visual palette, launch stats) are the owner's/designer's to fill in outside this repo's
build process. The three long-standing owner-only items (D-009 legal review, D-010 privacy policy —
now also blocking the onboarding redesign directly, not just a parked item — and `ANTHROPIC_API_KEY`
cloud-env persistence) remain open.
