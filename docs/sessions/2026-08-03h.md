# Session 2026-08-03h

**What:** Owner asked what's left to do; surveyed the project (Phase 1 prompt-validation is now
fully clean — FINDING 9/10/11 all resolved — but `app/` is still empty and D-012 left four
consequences undesigned: trigger logic, micro-capture flow, onboarding, manual fallback). Owner
picked D-012's onboarding/capture design to scope this session.

Classified the four sub-problems rather than designing them all at once: trigger logic and the
onboarding redesign both carry real weight (trigger logic is compliance-shaped, same family as
BRIEF-002/D-028/BRIEF-006; onboarding shares BQ-004's "no interface exists yet" problem); micro-
capture flow is governed by an existing principle (P1) and can likely be handled as a normal Tier-2
pass; manual fallback is low-stakes and can wait for real screens. Owner chose trigger logic as this
session's actual work.

Ran it through the trigger checklist before writing anything: fires trigger 2 (advisory line — same
shape as the deepen-selection question) and trigger 3 (interprets P2/D-032). Routes to Tier 3, no
lens work. Wrote **`docs/BRIEF-007_surfacing_trigger_logic.md`** — the key scoping finding is that
"trigger logic" is actually two separable problems: WHICH product type is a candidate to surface
(mechanical, backend-computable today against existing Income/Goal/Holding schema, no interface
needed) versus WHEN a given conversational turn is the right moment to raise one (needs D-032's
on-topic test extended and re-verified, closer to conversational judgment). Modeled three paths
(mechanical candidate-list + fixed tie-break; leave both to the model; defer surfacing from MVP
entirely) with no recommendation — not asked for one this time. Rule extraction: a backend-computed
candidate *list* narrows but doesn't by itself close the structural-prioritization gap if more than
one candidate can be simultaneously eligible — the tie-break needs the same scrutiny too.

**What's next:** BRIEF-007 awaits an owner decision, same as BRIEF-006 did. No code touched this
session. Once resolved, micro-capture flow (Tier 2, governed by P1) is the natural next D-012 piece.
