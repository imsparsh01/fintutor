# Session — 02-Aug-2026 (fourth session)

**Task:** Execute the owner's decision on BRIEF-004 — Path A (narrow patch) adopted for the gap-surfacing
scope question.

**What changed:**
- Logged **D-035** in `docs/DECISION_LOG.md`: Path A adopted. Interprets D-032 — the on-topic gap-surfacing
  constraint governs the whole answer, not only the closing offer.
- `docs/prompts/TEACHING_SYSTEM_PROMPT.md` §2 rule 3: two new paragraphs. First, extends the existing
  on-topic test ("is this thread already in the room, or a new door?") to anywhere in the answer, closing
  the mid-answer insertion channel FINDING 10 used. Second, an explicit guard clause stating this does not
  loosen rule 2's requirement to name every already-relevant holding — written specifically to guard against
  the cross-contamination risk BRIEF-004 flagged (this fix making FINDING 9 worse).
- Regenerated `docs/prompts/SYSTEM_PROMPT_v0_7_runnable.md`. Diffed against v0.6 before writing: only the
  header changelog comment and the two new §2 paragraphs changed — §1, §3, §4, §5 byte-identical.
- `PROJECT_SPEC.md` §8: BRIEF-004 marked resolved (D-035). Added a new, explicit open item for **FINDING 9**
  — deliberately NOT resolved by this decision, still needs its own path.
- `docs/BUILD_QUEUE.md`: added **BQ-007** to READY — re-run the same n=5 Q1 repeat series against v0.7 to
  confirm FINDING 10 is actually closed and, critically, that FINDING 9's 40% rate did not get worse. Per
  D-035's own escalate-if clause: if FINDING 9 worsens, that goes back to thinking-home as a new finding, not
  a footnote on this one.

**Why this isn't "done" yet in the fullest sense:** consistent with how every prior prompt fix here
(D-025→Run 2, D-028→Run 2/4, D-032→Run 3/4) was only treated as closed after a real run confirmed it, this
decision is logged and the prompt is regenerated, but BQ-007 (untested) is what actually confirms the fix
holds and didn't trade one problem for the other.

**What's next:** Run BQ-007. FINDING 9 remains a fully separate open item regardless of BQ-007's outcome —
it was never in scope for BRIEF-004/D-035 and needs its own brief when picked up.
