# Session 2026-08-03b

**Task:** BQ-009 — Add Income and Goal objects to the backend baseline schema.

## What changed

- **`backend/app/models/income.py`** — `Income` model: `id` (UUID PK), `user_id` (loose UUID, no FK),
  `sources` (JSONB list of `{label, amount, frequency}`).
- **`backend/app/models/goal.py`** — `Goal` model: `id`, `user_id` (loose UUID, no FK), `target_amount`,
  `target_date`, `category`. `GoalFunding` child table backing `funded_by`: `goal_id` (real FK to
  `goals.id`, cascade delete), `holding_id` (loose UUID, no FK), `earmarked_amount`. No progress field
  stored on `Goal` — computed live from `funded_by`, per D-038.
- **D-043 (Tier 1) — gap surfaced and resolved.** BQ-009 was queued as "sibling to Holdings," but no
  Holdings table exists anywhere in the codebase yet (`models/` was empty going in). Asked the owner
  before writing code: chose loose UUID references (no FK) for `holding_id` and `user_id` rather than
  building a Holdings/Users stub to unblock this session. See D-043 in `docs/DECISION_LOG.md`.
- **Alembic migration `069bc85fc512`** generated via autogenerate, reviewed by hand, applied against the
  real Supabase DB.
- **`docs/BUILD_QUEUE.md`** updated: BQ-009 moved to DONE; BQ-010 flagged with a note that Holdings still
  doesn't exist, so its "read live off holding records" half has nothing to read from yet.

## Verification

- App imports cleanly with the new models registered.
- Migration applied with no errors against the live database.
- Round-trip test: inserted an `Income` row, a `Goal` row, and a `GoalFunding` row against the real DB,
  read the cascade relationship back correctly, deleted all three — no errors.
- `/health` → 200, `/health/db` → 200 with the new models loaded.

## Why

Decision 3 (D-038) resolved the data model last session; this session executes the first piece of it.

## What's next

- **BQ-010** — Implement live budget computation. Depends on BQ-009 (done) and BQ-011 (done), but flag to
  the owner at session start that Holdings doesn't exist yet — the "recurring outflows read live off
  holding records" half of BQ-010 has nothing to read from until a Holdings model exists. Likely needs a
  quick owner call on sequencing (build a real Holdings model first vs. scope BQ-010 down to
  income-minus-discretionary-only for now).
- Standing open items, unchanged: legal review of D-009, data privacy policy (D-010), BQ-004 (blocked on
  the `deepen`-selection rule), Decision 2 (per-item management depth), the D-013 idle-cash taxonomy
  question.
