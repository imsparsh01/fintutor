# Session 2026-08-09

**What:** Resumed session, confirmed full git sync (`origin/main` already matched local `HEAD` — nothing
unpushed, nothing unpulled), then built **BQ-042** end to end: the onboarding structured conversation flow
confirmed by D-084.

**Note on the previous session:** commits D-083 and D-084 (resolving the PRD's cross-turn-state fork and
confirming the PRD/queueing BQ-042) landed without a `docs/sessions/` entry — a gap in that session's own
checklist, not this one's. Recorded here for the trail: D-083 adopted a narrow stage indicator (Path A,
never conversation memory — D-022 stays parked); D-084 confirmed the PRD's stage/path map, persisted-state
shape, and fail-safe mechanics as proposed and queued BQ-042.

**BQ-042 built.** Backend: new `OnboardingState` model (`backend/app/models/onboarding_state.py`) + Alembic
migration, modeled directly on `StreakState`. New `backend/app/services/onboarding.py`: track routing on
the first turn (deterministic chip-tap hint, mirroring D-071's `deepen_alias` pattern, or a narrow Haiku
classifier for free-typed messages, mirroring D-072's `classify_deepen`) and stage advancement after each
turn (a second Haiku classifier judging whether the stage's goal was met), backed by a hard turn-budget
fail-safe (4 turns per stage, 1 for `unclassified`'s routing `intro`) that force-completes the stage
regardless of classifier output — the actual guarantee behind the PRD's "no turn may leave the user with
nowhere to go" requirement. `/chat`'s `ChatRequest`/response extended to carry `onboarding` / `track` /
`stage`, gated so every non-onboarding caller (general Chat tab, `HoldingDetailScreen`) is untouched. System
prompt (`docs/prompts/SYSTEM_PROMPT_v0_8_runnable.md` §4) documents the new `onboarding` field, explicit
that it's a position marker, never a transcript. Frontend: `OnboardingScreen`'s four chips now each name
their track; `ChatThread`/`app/lib/chat.ts` thread it through to the backend.

Full detail — including the exact stage/path map, the BRIEF-011 compliance-note wiring, and what got
resolved from the PRD's own "not yet written" list — is in `docs/BUILD_QUEUE_ARCHIVE.md`'s BQ-042 entry and
`docs/features/onboarding/PRD.md`'s new "Built" section.

**Verification and its limit:** Python syntax/imports clean, `tsc --noEmit` clean across the whole app, and
the onboarding service's persistence + turn-budget logic verified directly against a real (non-mocked)
SQLite session — both the deterministic-hint and classifier-fallback paths, and the 4-turn / 1-turn budgets
firing exactly on schedule. **Not live-verified against a real Postgres DB or the live Anthropic API** —
this cloud session still has neither `DATABASE_URL` nor `ANTHROPIC_API_KEY` configured, the same standing
gap noted in prior sessions. An HTTP-level `TestClient` smoke test hit a pre-existing SQLite-vs-Postgres
`UUID` type incompatibility shared by every model in this schema (not something BQ-042 introduced). Real
end-to-end verification (real DB, real API key, real device/browser) is the owner's local-session task, same
pattern as every prior DB-touching build item.

**`docs/BUILD_QUEUE.md` changed this session** — BQ-042 moved from READY to `docs/BUILD_QUEUE_ARCHIVE.md`
as done, per D-081's per-completion habit. READY is now empty. `docs/DECISION_LOG.md` and `PROJECT_SPEC.md`
§10 both unchanged this session (no new decisions — this was pure execution of an already-confirmed build
item); DECISION_LOG.md is at 19/~20 entries, still within its rolling window, no archiving needed.

**Worth flagging to the owner:** `PROJECT_SPEC.md`'s §8 "Design AI-surfacing trigger logic + micro-capture
flow (D-012)" item lists "a re-thought AI-driven onboarding mechanism" as one of four undesigned sub-parts.
BQ-042 substantially addresses that sub-part, but the item bundles three other sub-parts (surfacing trigger
logic, progressive structured-field capture, manual fallback UI) that this session didn't touch — not
proposing a checkbox edit without the owner's read on whether that's a fair partial-close.

**Same session, continued: `PROJECT_SPEC.md` §8 audit + checkoff.** With `BUILD_QUEUE.md` READY empty, did
an audit (owner-directed: "Design AI-surfacing trigger logic") of D-012's four originally-undesigned
consequences rather than treating it as fresh design work — all four turned out already closed, just never
recognized as a set: WHICH (candidate selection) by D-051/BQ-013, WHEN (moment selection) by D-080's live
Phase-1 Run 7 verification, micro-capture by D-078/BQ-039, onboarding re-think by this session's own
D-082–D-084/BQ-042, and the manual fallback UI by BQ-036 (shipped earlier, before this item was
revisited). Owner confirmed; checked off in `PROJECT_SPEC.md` §8 with the full trace, plus a §10 change-log
entry (v3.9). No new decision — pure housekeeping, explicitly framed that way in both places. Committed
separately (`77693e9`) and fast-forward-merged to `main` on request.

**Then: external design references saved.** Owner asked (casual, not a build/design task) what similar
apps exist for UI/UX inspiration. Researched live via web search rather than relying on possibly-stale
training knowledge; new `docs/ux/DESIGN_REFERENCES.md` catalogues the results — grouped by which slice of
FinTutor's UX each is closest to (Cleo for the chat-first interaction shell; INDmoney/Groww/ET Money for
category/consolidated-portfolio layout; YNAB/Monarch/Copilot for teaching voice and dashboard polish;
Fi Money/Jupiter/Richify noted but explicitly flagged as not verified firsthand this pass). Framed
explicitly as inspiration only, same explicit-ask-only posture as the vendored `design-taste-frontend`
skill (D-063/D-064) — not a decision, not scope, not a commitment to any pattern. `docs/ux/README.md` got
a one-line pointer added so the folder's own "what goes where" map stays accurate.

**What's next:** No item in `docs/BUILD_QUEUE.md` READY. Real device/browser + live-DB verification of
BQ-042 is the natural next step on the owner's local session — this session's `ANTHROPIC_API_KEY`/
`DATABASE_URL` gap makes that a local-only task for now. The three long-standing owner-only items remain
open: D-009 legal review, D-010 privacy policy, and that same cloud-env credential persistence question.
`docs/ux/DESIGN_REFERENCES.md` is sitting ready for whenever the aesthetic layer actually gets designed —
not itself a next action, just no longer something to research from scratch when that day comes.
