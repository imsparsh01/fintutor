# Session 2026-08-05a (checkpoint — live discussion in progress, not closed)

**This is a mid-discussion checkpoint, not a session close.** Owner asked to commit/push so a fresh session
can resume the conversation exactly where it left off, without replaying it. No file other than this one has
changed — `PRODUCT_PRINCIPLES.md` itself is untouched; nothing below is confirmed yet.

**Context:** continuing from 2026-08-04j (deepened live-verification pass) and 2026-08-04k (BQ-038 + main-
branch fix). Owner asked to open the standing backlog item — the UX/interaction-model principles section in
`docs/PRODUCT_PRINCIPLES.md` D-031 anticipated (unblocked since Decisions 2/3 resolved, still unwritten) —
as a live, talked-through discussion rather than a unilateral draft, matching the file's own discipline
("extracted from decisions already made, not invented") and the precedent set by how P7 was built (cluster
by cluster, tension named explicitly, owner confirms each piece).

## Ground laid before the live discussion started

`PRODUCT_PRINCIPLES.md`'s "deliberately unprincipled" note names five UX characteristics D-031 implied:
persistent category sections, AI-primary/manual-secondary population, aliases never shown, no comprehension
gates, progressive capture. Checked each against what already exists:

| Item | Status |
|---|---|
| Aliases never shown | Already covered — this is P6 verbatim |
| AI-primary/manual-secondary population | Mostly covered by P1, but P1's "Traced to" note doesn't reflect that D-031 later partially superseded D-012 (menu-as-secondary is now allowed; D-012's primacy commitment survives) — needs a patch, not a new principle |
| Progressive capture | Largely P1 territory too (D-012's "capture details only on user interest") |
| Persistent, always-accessible sections | Genuinely new — nothing currently covers this information-architecture stance |
| No comprehension gates (no quizzes/lesson-tree to unlock) | Genuinely new — traces to `PROJECT_SPEC.md` §2's "Learn on the go... no curriculum" line, founding-spec language never promoted to a checkable Product-lens test |

**Conclusion reached with the owner:** the real deliverable is smaller than "five new principles" — one P1
patch, plus two genuinely new principles (persistent sections; no comprehension gates). Owner chose to talk
through each live, one at a time, starting with the P1 patch as the easiest.

## Where the live discussion actually is right now

**Item 1 of 3 — P1 patch — proposed, NOT yet confirmed by the owner.** Exact proposed addition to P1's
"Traced to" note in `PRODUCT_PRINCIPLES.md`:

> *Narrowed by D-031: a manual/browse entry point into the same sections is permitted as the secondary
> path — this principle governs which path is primary, not whether a fallback may exist.*

The open question put to the owner, unanswered when this checkpoint was written: **does that read right, or
would they phrase the boundary differently?**

**Item 2 (not started) — persistent, always-accessible sections.** Will need extraction from D-031 the same
way. One tension already flagged, to raise explicitly when this item comes up (per this file's own
discipline of naming tension rather than smoothing it): does "persistent" apply to an *empty* section (no
holdings yet) the same as a populated one, or does that pressure toward inventing UI before there's data to
show?

**Item 3 (not started) — no comprehension gates / no lesson-tree.** Traces to `PROJECT_SPEC.md` §2/§4's
"Learn on the go" language. No tension identified yet — first pass not done.

## How to resume this exact conversation

A fresh session should: read this file, then re-open with Item 1 — restate the proposed P1 patch above and
ask the owner the same phrasing question, rather than re-deriving the cross-check table from scratch (already
done, captured above). Once Item 1 is confirmed, move to Item 2 with its flagged tension, then Item 3. Only
write to `PRODUCT_PRINCIPLES.md` once each item is actually confirmed — per the file's own rule, a principle
must be checkable without the owner in the room, so getting the wording right before it's committed matters
more than speed here.

**`docs/DECISION_LOG.md` and `PROJECT_SPEC.md` are untouched.** No BUILD_QUEUE change — this isn't a build
task.
