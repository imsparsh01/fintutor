# Session — 2 Sep 2026

## BQ-154 authenticated Calculator QA checkpoint

- Confirmed the owner-authenticated production web session and completed onboarding through the Calculator starting point without inventing financial-profile answers.
- Exercised Emergency Coverage against configured Supabase data: component provenance, excluded-by-default candidates, explicit Include, frozen result evidence, reset isolation, exact Arya payload, Cancel/no-send and result/opener focus all passed.
- Stopped the API deliberately to prove manual offline calculation (₹1,20,000 ÷ ₹20,000 = 6.0 months), retryable candidate failure, no automatic replay after reconnect and explicit-retry recovery with manual drafts preserved.
- Fixed the local QA CORS rule to accept only `localhost` or `127.0.0.1` on a local port; added regression coverage for both. This resolved the authenticated onboarding preflight failure.
- Fixed the shared Arya handoff modal so web Escape dismisses it with listener cleanup; the rebuilt production bundle passed the configured Escape and exact-opener-focus check.
- Verification: 137/137 frontend tests PASS; 402/402 backend tests PASS; TypeScript PASS; production web export PASS; configured authenticated walkthrough PASS for the evidence rows marked `PASS-LIVE`; `git diff --check` PASS.
- BQ-154 remains open only for 320/390/1440/200%, theme/high-contrast/reduced-motion, console and best-effort native observations, followed by owner PASS / REVISE / PARK / ESCALATE.
- No decision, schema, route, formula, persistence, dependency, catalogue scope, `PROJECT_SPEC.md` or `docs/DECISION_LOG.md` change.
