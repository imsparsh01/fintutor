# Session 2026-08-20 — simulator walkthrough setup

## What changed

- Resumed after a gap; confirmed local `main` is fully in sync with `origin/main` — nothing was left
  unpushed from prior sessions.
- Fixed the backend venv for Python 3.14: the system Python was upgraded from 3.12 to 3.14, which
  broke the old venv. Rebuilt it; fixed three pip-install blockers:
  - `psycopg2-binary==2.9.9` has no Python 3.14 wheel → upgraded to `>=2.9.12`
  - `sqlalchemy==2.0.35` crashes on Python 3.14 with a `typing.Union.__getitem__` error → upgraded to `>=2.0.36`
  - `tzdata` was absent on Windows (Python 3.14's `zoneinfo` needs it) → added as new dependency (D-147)
  - Updated `requirements.txt` to reflect all three (D-147).
- Fixed `.claude/launch.json`: backend `runtimeExecutable` was `backend/.venv/bin/python` (Unix path);
  corrected to `backend/.venv/Scripts/python` for Windows.
- Cleared the dev-user bypass from `app/.env` (`EXPO_PUBLIC_DEV_USER_ID` / `EXPO_PUBLIC_DEV_USER_NAME`
  blanked) so the app shows the real login screen for the D-125 simulator walkthrough.
- Wrote and ran `backend/scripts/seed_rishab.py`: seeds all 12 tables with realistic data for
  `rishab.sharma@gmail.com` (10 holdings, 4 goals, income, budget categories, completed assessment,
  streak, progression, financial context). Safe to re-run.
- Both servers confirmed running: backend at `:8000`, Expo web at `:8081`.

## What's next

- Owner completes the D-125 simulator walkthrough at `http://localhost:8081` (log in as
  `rishab.sharma@gmail.com`).
- After walkthrough, document any failures found and decide whether to fix before or after external
  activation testing.
- D-120 autonomous pipeline questions (1–4) remain unanswered.
