# Session 2026-08-05b

**What:** Long session, six real pieces of work, resumed from 2026-08-05a's checkpoint.

**Closed the live UX-principles discussion (2026-08-05a).** Confirmed one item at a time with the owner:
P1's provenance note patched for D-031's manual-path narrowing (**D-075**); new **P8** — a holding
family's section is always reachable, never gated behind having data (**D-076**); new **P9** — no
comprehension gates, with an explicit boundary against P7's gamification toolkit being read as licensing
one (**D-077**). `PROJECT_SPEC.md` §8's UX-principles item checked off, resolved.

**BQ-039 — D-012's actual AI-surfaced holding-capture primary path, built for the first time.**
`docs/decisions/D-078-holding-capture-mechanism.md`: a narrow Haiku call (first real use of D-002's
"reconciliation" half, same shape as D-072's `deepen_classifier`) proposes a holding extraction from the
user's own chat message; a confirm-card UI gates every write, never auto-creates. Unit-tested against a
mocked client, then later live-verified for real (see below).

**D-079 — `savings_balance` resolved: not a missing 9th taxonomy type**, an instance of D-031's
already-deferred Cash & bank family. No build work, closed a question open since 25-Jul.

**Live Anthropic API access confirmed working in this environment, correcting a standing false
assumption.** The owner provided a real key (session-local `.env`, gitignored, never committed — verified
by grepping full git history afterward). Ran D-051's overdue WHEN-stage re-verification live (Q7 n=5
against v0.8: FINDING 8 does not reproduce, **D-080**). Caught a real bug in the process — `anthropic==
0.39.0` + current `httpx` crashes on first real use, never caught before because every prior test mocked
the client — fixed by pinning `httpx<0.28` (**BQ-040**). Then live-verified all three AI-calling functions
(`ask_teaching_engine`, `classify_deepen`, `classify_holding_capture`) directly, closing a disclaimer
carried since BQ-023 (**BQ-041**). No further bugs found.

**D-081 — governance-file restructuring for token efficiency, owner-authorized directly.** Measured the
real cost first (~59,773 mandatory session-start tokens), then: retroactively extracted D-001–D-044 (46
entries) out of `DECISION_LOG.md`'s old inline format into `docs/decisions/`, verified byte-for-byte
identical to the original; put a rolling window on the live log (~20 entries, rest in
`docs/DECISION_LOG_ARCHIVE.md`); moved `BUILD_QUEUE.md`'s 44-entry DONE section wholesale to
`docs/BUILD_QUEUE_ARCHIVE.md`; built `docs/DECISION_PROTOCOL_CHEATSHEET.md` (full protocol untouched,
still authoritative); gave `PROJECT_SPEC.md` §10 the same rolling-window treatment. Updated `CLAUDE.md`
and the `session-close` skill so archiving is a standing habit, not a one-time cleanup. Measured after:
~11,907 tokens, ~80% reduction. Full before/after table: `docs/sessions/2026-08-05-token-optimization.md`.

**`docs/DECISION_LOG.md` and `PROJECT_SPEC.md` both changed significantly this session** — worth a skim,
not just the D-081 restructuring but the six substantive decisions (D-075–D-081) themselves.

**What's next:** `BUILD_QUEUE.md` READY is empty. Remaining open items all need the owner: legal review
of D-009 (external), the data privacy policy (D-010, unwritten), and setting `ANTHROPIC_API_KEY` at the
environment level (not just this session's `.env`) if live API access should persist across sessions. The
owner also asked for a verification prompt to run at the start of the next session to confirm the token
reduction actually holds — that's the first thing the next session should do.
