# Session 2026-08-04j

**What:** Owner asked to pick up "a real device/simulator pass" from 2026-08-04i's standing backlog, and
separately asked whether Expo/a simulator could be bypassed in favor of an APK on a real phone. Investigated
sandbox capabilities first rather than assuming either way, then ran a deepened version of last session's
browser-based live-verification pass covering everything it didn't reach. No product code changed — this
was a verification-only session.

**Capability check (not assumed, actually tested this session):** no `/dev/kvm` — an Android emulator
cannot run here regardless of the Android SDK, Docker, or anything else installed; no iOS Simulator is
possible at all on this Linux container (Xcode-only); no physical device is reachable from this cloud
sandbox. Network policy still 403s `supabase.co` and `expo.dev` directly (confirmed via the proxy's own
status log). Owner chose to deepen the existing real-Postgres + real-backend + real-Chromium rig rather than
attempt an APK build (which would need a reachable backend from a real phone — its own infra decision, not
taken up this session).

**The pass itself.** Started local Postgres 16 (already installed, just not running), ran all four Alembic
migrations clean against a fresh `fintutor_test` DB, started the real FastAPI backend against it, `npm
install`ed the app once (`node_modules` wasn't present), temporarily added `react-dom`/`react-native-web`/
`@expo/metro-runtime` for the web build (same as 2026-08-04i, reverted after), and drove the real Expo web
build with a real headless Chromium via Playwright — Supabase auth mocked locally (browser-level route
interception returning a fake GoTrue session/token, since `supabase.co` is unreachable), everything else
hitting the real stack.

**New this session vs. last time:** last session's pass covered onboarding, all three holdings tabs,
Budgeting, Chat, Holding Detail, loan-vs-invest, and "Ask about this." This session specifically targeted
what that pass didn't reach — the four items only unit-tested until now:
- **ESOP exercise-cost modal:** seeded a real ESOP holding (4,800 units, 30 months elapsed, 12mo cliff, 48mo
  vest, strike ₹50, FMV ₹120) and hand-computed the expected output first. Live UI showed vested units
  3,000 of 4,800, cash needed ₹1,50,000, taxable spread ₹2,10,000 — exact match.
- **Tax-saving-room modal, both regimes:** seeded PPF (₹60,000/yr) + term insurance (₹12,000/yr premium).
  Old regime live-showed ₹78,000 unused 80C room (hand-computed: ₹1,50,000 − ₹72,000) — exact match. New
  regime correctly showed the "not relevant for you" message with no number.
- **Discretionary-category CRUD (BQ-037), the first live check it's ever gotten:** added "Eating out" /
  ₹6,000 through the actual form. The category appeared in the list AND the Budget card's Discretionary
  total and Net both updated live (₹0→₹6,000, ₹79,000→₹73,000) — confirming `compute_budget()` really does
  read this table, not just that the route exists.
- **Streak/mascot/reward chain, end to end:** rewound `streak_states.last_active_date` in the real DB to
  simulate day-over-day opens against the live `/streak/open` endpoint — confirmed consecutive-day
  increments, a missed-day reset that correctly preserves `longest_streak`, a same-day repeat correctly
  no-op'ing (can't be gamed by refreshing), and a reward rate close to the designed 30% over n=10 (3/10
  fired). Then reproduced it in the actual browser: reloading the real Consolidated screen after a rewound
  "yesterday" caught the mascot mid-reaction on the first attempt — 🌿 "Nice one!" rendered live, not just
  asserted from a mocked response.

**One real, unlogged gap found and worked around, not fixed:** the backend has no CORS middleware, so the
browser blocked every request to it ("Failed to fetch") until the test Chromium was launched with
`--disable-web-security`. This never mattered before because native `fetch()` isn't CORS-constrained, and it
doesn't matter for the shipped product (web isn't a deployment target) — but it would have silently blocked
*any* future browser-based verification pass without this note. Logged as a new LOW item in
`docs/KNOWN_LIMITATIONS.md`; explicitly not fixed (adding real CORS config is its own decision if web is
ever a real target, not implied here).

**Verified live, all four correctly:** ESOP exercise-cost math, both tax-saving-room regimes, discretionary-
category CRUD's effect on the Budget card, and the full streak/reward/mascot loop (backend logic + real
browser rendering). No bugs found this session — everything matched its hand-computed or previously
unit-tested expectation exactly.

**Cleanup, same discipline as 2026-08-04i:** killed the backend/Expo processes, stopped Postgres, dropped the
`fintutor_test` DB/role, deleted the temporary `.env` files (root + `app/`), reverted
`app/package.json`/`package-lock.json` to drop the temporary web-testing dependencies, and removed
`backend/.venv`/`app/node_modules`. `git status` is clean.

**`docs/DECISION_LOG.md` and `PROJECT_SPEC.md` were NOT touched this session** — nothing here was a decision,
just verification of already-shipped, already-decided work. `docs/KNOWN_LIMITATIONS.md` gained one new LOW
entry (the CORS note above).

**What's next:** `docs/BUILD_QUEUE.md`'s READY and BLOCKED are still both empty — same standing backlog as
2026-08-04i closed with: the data privacy policy (D-010), legal review scheduling, the UX principles
section, and the two pre-existing LOW findings (stale `consolidated.py` comment, raw Chat error message).
The device/simulator gap itself is now about as closed as this sandbox structurally allows — an APK-on-a-
real-phone pass remains a real option but needs its own infra decision first (how the phone reaches a
running backend) if the owner wants to pick it up later.
