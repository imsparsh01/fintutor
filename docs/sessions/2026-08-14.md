# Session 2026-08-14 — parallel pending-work reconciliation

## What changed

- Audited frontend, backend/data, and governance/status in parallel against the approved decisions,
  live build queue, known limitations, and current source.
- Confirmed the backend suite passes 311/311 and the frontend passes TypeScript plus 30/30 Node tests.
- Corrected two stale status artifacts: the backend codemap no longer claims strict income cadence, and
  `TODOS.md` no longer says Jest is required before any frontend unit coverage.
- Refreshed `DECISIONS_FOR_YOU.md` so it no longer claims the empty queue has work in it.

## Newly recovered backlog

- D-128 approved five calculators, but only three shipped. Goal Affordability and Term-insurance Coverage
  are stranded before their required formula/input/disclosure contracts; neither is mechanically buildable.
- Frontend reminder work can report save/delete failure after the backend mutation already succeeded if
  local notification work fails. This is a bounded reliability candidate, but it is not in READY.
- The fail-closed promises in the deepen and holding-capture classifiers lack direct malformed-response and
  client-construction coverage. Direct baseline/income/discretionary/reward coverage is also thin.
- Health Score's device-local emergency/insurance answers use global keys and can cross accounts on one
  installation. Its fix overlaps the owner-gated data-home/privacy decision.

## Owner-gated resume list

1. D2: data home for dependents / emergency-fund context.
2. D3: Supabase JWT ownership boundary.
3. D4: confirm or correct the shipped monthly reminder rule.
4. Scope the D-128 Goal Affordability and D-132 Term-insurance contracts into bounded decisions/tasks.
5. Decide the eight money/tax/streak defects recorded in `2026-08-12z.md`.
6. Resolve the Health Score cross-account local-state boundary.
7. Complete D-010 privacy policy and required pre-launch legal reviews.
8. Perform D-125 owner simulator validation before BQ-072/external activation testing.

## Sync state

- Changed: `docs/CODEMAPS/backend.md`, `TODOS.md`, `DECISIONS_FOR_YOU.md`, this session log.
- `PROJECT_SPEC.md` header is stale (`v4.1` versus newest changelog `v4.2`), but it is propose-only and was
  not edited without owner confirmation.
- `docs/DECISION_LOG.md` has 28 entries and needs its next rolling-window relocation; no decision entry was
  added this session, so no decision content was rewritten during this audit.
- No REVIEW-FLAGGED Tier-2 decision was made.

## Continued — D2 resolved

- Owner chose Path B: one dedicated, minimal financial-context record per user (D-134).
- First approved values: nullable non-negative financial-dependant count and nullable self-reported
  emergency-fund months. Both require explicit confirmation; onboarding cannot infer either value.
- Implementation remains gated by D3 authenticated ownership and D-010 retention/deletion/backup treatment.

## Continued — delivery tracking adopted

- Owner chose Path B and approved D-135: every decision now requires a delivery disposition.
- Added `docs/DECISION_DELIVERY_TRACKER.md` and recovered the open build obligations into BQ-085/086/087.
- Updated the deliberate-only session-close rules so a missing disposition is a close failure.

## Continued — token-lean historical reconciliation

- Owner confirmed D-136: preserve D-081 by indexing first and persisting only delivery exceptions.
- Recovered D-119's missing assessment view/change/clear UI (BQ-088 READY) and stale session-close skill
  maintenance (BQ-090 READY).
- Added explicit blocked delivery items for backend JWT ownership (BQ-089), mandatory gstack gate repair or
  supersession (BQ-091), and the unresolved production hosting target (BQ-092).
- Added D-095 production cleanup and D-123 recap/profile-coverage branches as explicit DEFERRED obligations.

## Continued — exhaustive D-001–D-136 audit

- Completed an obligation-level audit of every decision ID, with independent range owners and an adversarial
  narrowing/supersession pass. Compact checkpoints live in `docs/DELIVERY_AUDIT_MANIFEST.md`.
- Recovered unbuilt/partial commitments including all-type in-context surfacing, the Hook Loop trigger layer,
  tax/HRA calculators, scheduled progression pruning/account deletion, and missing calculator disclosures.
- Recorded deliberate later families/scenarios as deferred and implementation divergences for consolidated
  valuation metadata and reminder days rather than treating prior DONE items as full conformance.

## Continued — READY implementation batch

- Shipped BQ-088 assessment context management, BQ-090 session-close synchronization, and BQ-094 decision
  artifact repair. Frontend verification: TypeScript, 33 tests, and Expo web export; backend: 311 tests.
- BQ-101 partially advanced: SIP Goal now discloses D-129 timing. Review found Step-up SIP compounds each
  contribution immediately, contradicting D-129; no false disclosure or unauthorized formula change was
  made. BQ-101 moved to BLOCKED for owner approval.

## Continued — BQ-101 unblocked and completed

- Owner reconfirmed D-129 Option A. Corrected Step-up SIP to add each contribution after monthly growth,
  preserving the existing annual-step boundary, and added truthful timing disclosures to both calculators.
- Extracted the formula into `stepUpSip.ts` with focused month-end, annual-step and invalid-input tests.
# D-137 — Supabase JWT-derived backend ownership

- Owner chose Option A for D3: every protected backend request will be authenticated with a Supabase bearer
  token, and the verified token subject—not a caller-supplied `user_id`—will determine ownership.
- Multiple test accounts remain supported and BQ-089 requires explicit cross-account isolation tests.
- Recorded D-137 and moved BQ-089 from BLOCKED to READY. No authentication code was changed in this
  decision-only step.

# BQ-089 — Supabase JWT ownership shipped

- Protected backend routes now validate the Supabase bearer token and replace any supplied `user_id` with
  its verified subject before route parsing; health and API-schema routes remain public.
- Every frontend user-data API wrapper now uses one authenticated fetch primitive and sends no user ID over
  HTTP. Multiple signed-in accounts remain supported.
- Added real verifier and two-account boundary tests covering missing/rejected tokens, spoofed IDs,
  cross-account read/write/delete denial, and token-owned creation.
- Removed the resolved JWT limitation, refreshed all affected codemaps, archived BQ-089, and marked D-137
  shipped in the delivery tracker.

# D-138 — MVP at-rest protection

- Owner chose Supabase-managed database/backup encryption plus strict access, transport, network and secret
  controls for the MVP; FinTutor will not introduce separate field-encryption keys at this stage.
- Production SSL enforcement and applicable network restrictions are now explicit BQ-092 acceptance criteria.
- D-010 remains open for backup duration, deletion, disclosure/export, provider treatment and legal review.

# D-139 — Seven-day recovery-backup boundary

- Owner chose immediate active-data deletion with a maximum seven-day encrypted recovery-backup window and
  no additional long-lived manual backups for the MVP.
- Backup-contained data cannot be used normally, the privacy notice must disclose the delay, and any restore
  must reapply later account deletions before serving users.
- BQ-099 now carries this requirement; D-010 still needs the deletion experience/sequencing and
  disclosure/export/provider treatment decisions.

# D-140 — Reauthenticated whole-account deletion

- Owner chose fresh authentication plus a separate final confirmation for irreversible account deletion.
- The operation is data-first, backend-only for Supabase Auth deletion, idempotent and retry-safe; success
  appears only after application data and the Auth account are both gone.
- Moved BQ-099 to READY. D-010 still needs disclosure/export and remaining provider/legal treatment.

# BQ-099 — Whole-account deletion shipped

- Added the two-stage Home deletion UI, password reauthentication, complete data-first erasure registry,
  permanent backend-only Supabase Auth deletion, local sign-out/state clearing, and stable error handling.
- Tests cover the verified-subject boundary, exact confirmation, complete model registry, reauth failure,
  database failure preserving Auth, Auth failure after erasure, and repeated retry convergence.
- Added an isolated-restore runbook that reconciles against current active Auth users before any cutover.
- Runtime Auth deletion requires `SUPABASE_SERVICE_ROLE_KEY` only in backend `.env`; it is never public.

# D-141 — Sensitive-context disclosure placement

- Owner chose Option B: detailed treatment of dependant count and self-reported emergency-fund months lives
  in the privacy policy, not as long-form field-adjacent copy.
- The fields remain clearly labelled, optional, explicitly entered, and viewable/changeable/clearable.
- BQ-087's auth/retention/deletion/backup gates are settled; it remains blocked until the applicable final
  policy text and accessible in-app link exist.
- Supabase's official MCP was evaluated for future development operations. It is not currently connected;
  any connection must be development-only, project-scoped, and least-privilege.
