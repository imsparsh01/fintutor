# Session — 01-Aug-2026 (second session)

**Task:** BQ-001 — run Q7 and Q8 against `SYSTEM_PROMPT_v0_5_runnable.md`.

**What changed:**
- Added `docs/PHASE1_RUN3_RESULTS.md` — Q7 (memory claim) and Q8 (irrelevant-holding discipline) scored
  against `TEST_PROTOCOL.md`. Both run manually by the owner on Console Workbench (no scripted API access
  from build-home this session — no `ANTHROPIC_API_KEY` in the shell); outputs pasted back for scoring.
- Q8: clean pass, no findings.
- Q7: passes its own checklist (correctly denies memory, correctly distinguishes current-picture from
  chat-history) but produced a new finding — **FINDING 8**: the response closed by volunteering the
  term-insurance gap unprompted, using ranking language ("worth looking at"), on a question with zero
  financial content. This touches §3 rule 5 (never rank the user's problems) and PROJECT_SPEC.md's D-012
  Trigger A/B narrowing (cold/unprompted surfacing is out of MVP scope). **Not resolved here — flagged for
  thinking-home**, per BQ-001's own escalate-if clause.
- `docs/BUILD_QUEUE.md`: moved BQ-001 to DONE, noting the escalation.

**What's next:** BQ-002 (second fixture, no dominant number) is next in READY. Owner should decide whether
FINDING 8 needs its own BRIEF in thinking-home, and whether Trigger A/B scope should be written into the
prompt directly rather than relying on the model's own reading of "relevant to what they asked" (§4).

**Sync note:** No `PROJECT_SPEC.md` or `docs/DECISION_LOG.md` changes this session. `docs/BUILD_QUEUE.md` and
`docs/PHASE1_RUN3_RESULTS.md` are build-home artifacts, but **FINDING 8 itself is new information
thinking-home needs** — the owner should carry it into the Project even though no file here requires
re-upload.
