# Session 2026-08-04e

**What:** Short continuation of 2026-08-04d. Owner confirmed the D-063 vendoring approach was right, then
pinned down exactly when `design-taste-frontend` should ever be used: as design-philosophy inspiration
only when starting UI/design work on the FinTutor app itself — explicitly NOT for CEO_DASHBOARD.md/.html
or other personal-use asks — and only when the owner explicitly asks for it, never auto-triggered.

**Invocation policy pinned (D-064, Tier 1).** Owner-stated directly, no judgment call needed. Implemented
by editing the vendored skill's frontmatter `description` (the field that actually drives triggering) to
state the gate explicitly — the one disclosed deviation from D-063's verbatim-vendoring rule, recorded in
`.claude/skills/design-taste-frontend/PROVENANCE.md`'s new "Invocation policy" section. Document body left
untouched.

**`docs/DECISION_LOG.md` changed again this session** (D-064 appended). `PROJECT_SPEC.md` was not touched.

**Owner then asked to close the session** — skill discussion complete.

**What's next:** unchanged from 2026-08-04d — same standing backlog (BQ-028, streak/reward chain,
BQ-023/024, ESOP schema, variable-income budgeting, UX/interaction-model principles section).
`design-taste-frontend` now sits ready and correctly gated for whenever real FinTutor app UI/design work
begins.
