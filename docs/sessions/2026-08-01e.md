# Session — 01-Aug-2026 (fifth session)

**Task:** BQ-003 — run Q1 against both fixtures and compare.

**What changed:**
- Q1 run manually by the owner on Console Workbench against `SYSTEM_PROMPT_v0_6_runnable.md` (BQ-003 was
  queued against v0.5, but v0.6 shipped first this session — ran against v0.6 instead, a stronger test since
  it postdates D-028), fresh conversation per fixture, outputs pasted back for scoring.
- Added `docs/PHASE1_RUN4_RESULTS.md`. Headline result: **FINDING 4 does not reproduce in either fixture.**
  D-028's "deepen absent → deepen nothing, equal shallow treatment" guarantee held cleanly against both
  `FIXTURE_user_01.json` (Card-1's dominant 42%) and `FIXTURE_user_02.json` (the harder, ambiguous
  8.8%/13.5% case it was built to isolate) — no explicit path-selection language, no severity-based
  justification, no reference-frame capture in either run.
- **New finding (FINDING 9), not a reappearance of FINDING 4:** the user_01 run never mentions Card-1 at
  all — a completeness miss (§2 rule 2's "name every path"), not a ranking/over-attention failure. This is
  the opposite failure shape from FINDING 4 and doesn't match BQ-003's escalate-if clause (D-028's `deepen`
  field isn't implicated — it behaved correctly in both runs). Flagged for thinking-home, not resolved here,
  per the same pattern as FINDING 8.
- `docs/BUILD_QUEUE.md`: moved BQ-003 to DONE with the finding noted; READY is now empty.
- Owner's initial read on FINDING 9 was "real regression, worth its own fix." Before that went to
  thinking-home as a brief, ran a repeat series (n=5 total against `FIXTURE_user_01.json`, same setup, fresh
  conversation each time) to get an actual reproduction rate rather than act on n=1.
- **FINDING 9 confirmed at 2/5 (40%)** — not noise (one early clean repeat had briefly suggested otherwise;
  the full series doesn't support that read). Both misses share a pattern: neither goes straight to
  prepay-vs-invest, both substitute an emergency-fund/liquidity consideration for Card-1 rather than randomly
  dropping a path.
- **FINDING 10 (new), discovered during the repeat series:** one run (1/5) volunteered the term-insurance gap
  unprompted, mid-answer, on Q1 — a question that never touches insurance. D-032 pre-registered this exact
  outcome ("a question that is finance-adjacent but should still not trigger surfacing") as sufficient
  evidence on its own that Path B (the on-topic tightening) is insufficient, independent of rate. Flagged as
  the more urgent of the two findings.
- FINDING 4 / D-028's fix held across the entire repeat series — no path-selection language, no severity
  justification, in any of the 5 runs, including the 2 that dropped Card-1. Both findings are appended to
  `docs/PHASE1_RUN4_RESULTS.md` with the full 5-run table.

**What's next:** No item currently in READY. BQ-004 stays BLOCKED pending the thinking-home `deepen`
selection-logic decision. Owner should carry FINDING 9 and FINDING 10 into thinking-home; FINDING 10 in
particular may be sufficient on its own to move to D-032's Path A (broader Trigger-A/B gate), per that
decision's own stated logic — that call is thinking-home's, not build-home's.

**Sync note:** No `PROJECT_SPEC.md` or `docs/DECISION_LOG.md` changes this session — nothing to re-upload to
the Project. **FINDING 9 (now quantified) and FINDING 10 (new) are new information thinking-home needs**,
same as FINDING 8's sync note.
