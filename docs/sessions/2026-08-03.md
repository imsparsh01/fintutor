# Session 2026-08-03

**Task:** Resolve Decision 3 (budgeting/goals data model), then bootstrap the backend from scratch.

## What changed

- **D-038 — Decision 3 resolved.** Budgeting/goals data model: Goals hold explicit thin funding links to
  holdings (`funded_by: [{holding_id, earmarked_amount}]`, progress computed live, never duplicated).
  Budget is a fully computed view — no stored object; recurring outflows read live off holding records.
  New first-class `Income` object. Reference-vs-store test extracted as a reusable rule. Resolves
  `PROJECT_SPEC.md` §8 "Decision 3" (checked off, v2.8).
- **D-039/D-040/D-042 — CEO dashboard tooling.** Created `docs/CEO_DASHBOARD.md` (source data) and
  `docs/CEO_DASHBOARD.html` (a real, committed, double-clickable local visual snapshot — not just a
  claude.ai Artifact link), per the owner's request for an on-demand founder status view. D-040 initially
  added an automatic every-session refresh to `CLAUDE.md`'s checklist; **D-042 reversed that same
  session** (owner judged it more overhead than value) — the dashboard is now refreshed **on demand only**,
  when explicitly asked for. `CLAUDE.md`'s end-of-session checklist is back to its original 3 steps.
- **D-041/BQ-011 — Backend skeleton bootstrapped.** `backend/` was completely empty at session start (0
  files) — `BQ-009`/`BQ-010` as originally queued assumed a codebase to extend that didn't exist. Owner
  confirmed the stack (FastAPI + SQLAlchemy + Alembic) and session scope (skeleton only) via explicit
  questions before any code was written, per `CLAUDE.md`'s hard stop on introducing a new
  library/architecture silently. Built: FastAPI app, SQLAlchemy engine/session wiring, Alembic migrations
  tooling, `/health` and `/health/db` endpoints, `requirements.txt`, a local `.venv`. `BUILD_QUEUE.md`
  updated: `BQ-011` done, `BQ-009`/`BQ-010` marked as depending on it (now unblocked).
- **DATABASE_URL wired up and verified.** Owner added the Supabase Postgres connection string to `.env`.
  First attempt failed (malformed URL — an extra `@`, root-caused to an unencoded special character in the
  password); the error response briefly echoed a fragment of the password back through `/health/db`'s
  error detail, which surfaced in this session's tool output. Flagged to the owner as a burned credential;
  owner reset the Supabase DB password and re-added a clean `DATABASE_URL`. Re-verified: `/health` → 200,
  `/health/db` → 200, real connection confirmed.
- **Hardened `/health/db`'s error handling** (`backend/app/main.py`) so it can never echo a raw exception
  (which can embed the DSN) back to the caller again — now returns only the exception type name and logs
  detail server-side. Applied immediately after the leak was noticed, not deferred.

## Why

Decision 3 was the last foundational data-model gap blocking real build work (per `PROJECT_SPEC.md`'s own
"do before build" framing). Once resolved, picking up the queued build tasks surfaced that the backend
didn't exist yet at all — so this session became the actual start of Phase 2, not just schema additions.

## What's next

- **`BQ-009`** — Add Income and Goal objects to the backend baseline schema. READY, fully unblocked
  (skeleton exists, DB connection verified).
- **`BQ-010`** — Implement live budget computation. READY, depends on `BQ-009`.
- **Decision 2** (per-item management depth) — still waiting on a real Phase 1 app section to react to.
- Standing open items, unchanged: legal review of D-009, data privacy policy (D-010), `BQ-004` (blocked on
  the `deepen`-selection rule), the D-013 idle-cash taxonomy question.
- Mobile app (`app/`) still has 0 files — the next major milestone after the backend has real models.
