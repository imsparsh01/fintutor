# Session — 12-Aug-2026 (o)

## Changed

- Completed BQ-067: replaced four-track chat onboarding for new users with the approved five-axis,
  normalized frontend flow and explicit 18+ acknowledgement.
- Added deterministic single/multi-select handling, sentinel exclusivity, per-question skip, global exit,
  server resume, visible progress, retry states, Discovering-stage closing copy, and intent-based navigation.
- Made backend assessment state authoritative across devices; local storage caches handled status only as
  a temporary outage fallback. Assessment v2 never enters `/chat` or stores raw dialogue.
- Added a development-only onboarding preview switch and updated frontend/architecture codemaps.

## Verification

- `npx tsc --noEmit` — pass.
- `.venv/bin/python -m unittest discover -s tests -p 'test_*.py' -v` — 20/20 pass.
- `git diff --check` — pass.
- Browser QA passed on the isolated Expo web preview at a 390×844 mobile viewport. The eligibility surface
  fit without clipping and produced no runtime errors; the only console warning was Expo Notifications'
  known web-listener limitation.
- Review polish added a visible backend-state loading surface, native safe-area handling, semantic
  radio/checkbox/progress roles, alert announcements, tutor typography on explanatory closing copy, and
  preservation of already-dismissed legacy users until BQ-068 offers voluntary reassessment.

## Next

- BQ-068: grandfather legacy users and add voluntary reassessment without inferring v2 answers.
