# Session — 23-Aug-2026 (f)

Recorded the owner's Goal Progress Path B as D-150. Each recognized holding is capped once across all linked
goals; over-allocation scales proportionally to earmarks; unknown valuation remains explicitly unmeasured; and
currency rounding preserves the capped total deterministically.

Updated the baseline package, acceptance task and fixture to exercise a ₹2,00,000 holding split across
₹1,50,000 and ₹1,00,000 earmarks as ₹1,20,000 and ₹80,000. BQ-109 is now blocked only on the eight-task owner
walkthrough. No production code, schema, API or calculation changed.

`docs/DECISION_LOG.md` changed and should be skimmed.

## Gates

- **Pre-build:** scope was limited to recording the owner-selected rule and reconciling the existing BQ-109
  package/prototype. No production schema, API or calculation implementation was authorised.
- **Pre-commit review:** formula totals and unknown-value semantics match D-150; the fixture exercises
  over-allocation and the no-movement boundary; tracker/queue dispositions agree. No security, privacy or
  scope expansion finding remains.
- **Test:** `node --check` passes for the prototype, `git diff --check` passes, and stale open-rule copy was
  removed. Interactive owner validation remains the deliberate BQ-109 gate.
