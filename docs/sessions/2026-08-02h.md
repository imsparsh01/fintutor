# Session — 02-Aug-2026 (eighth session)

**Task:** Move ahead on the remaining open finding (FINDING 9) after BRIEF-005 closed.

**What changed:**
- Before writing another Tier-3 brief by default, ran FINDING 9 through the actual §2.1 trigger checklist.
  Conclusion: trigger 2 (compliance) does not fire — D-025 already settled that naming a collateral-relevant
  holding without ranking it is required, not just permitted, so this isn't a new compliance-line question.
  Trigger 3 fires (interprets D-015, classified product-judgment, not compliance), which per §4.3 routes to
  **Tier 2 with a mandatory REVIEW-FLAG** — not Tier 3.
- Ran the Tier 2 process properly: four-lens table (Compliance PASS, Product CONCERN, Technical PASS,
  Cost-and-Scope CONCERN — both CONCERNs pointing the same direction, not a deadlock), decision made, acted
  on immediately, logged as **D-037** with the full lens reasoning and a REVIEW-FLAG for the owner's
  retroactive veto — per protocol, Tier 2 does not wait for review before acting.
- `TEACHING_SYSTEM_PROMPT.md` §2 rule 2: new paragraph requiring a materially higher-cost holding (e.g.
  Card-1) to be named even when it isn't one of the two decision paths, and forbidding substituting a vaguer
  consideration (liquidity, emergency-fund framing) for it — targets the exact pattern all three FINDING 9
  misses (2/5 on v0.6, 1/5 on v0.7) shared.
- Regenerated `SYSTEM_PROMPT_v0_8_runnable.md`. Diffed against v0.7: only the header and the one new
  paragraph changed.
- `docs/BUILD_QUEUE.md`: added BQ-008 to verify the fix (same n=5 repeat pattern as BQ-007, also re-checks
  FINDING 10 wasn't disturbed and FINDING 11 against D-036's clarified test).
- `PROJECT_SPEC.md` §8 and change log updated.

**Why this is a different move than BRIEF-004/005:** those were both unambiguously compliance-category and
had to go to the owner. This is the first finding resolved without waiting — not because the bar was lowered,
but because applying the actual checklist showed it never needed Tier 3 in the first place. The REVIEW-FLAG
is what keeps this honest: it's visible to the owner and reversible, just not gated on their action before
proceeding.

**What's next:** BQ-008 needs to run (same local-script flow as BQ-007) before D-037 counts as confirmed
rather than just applied. FINDING 9 is provisionally closed pending that.
