# Session 2026-08-03c

**Task:** BQ-012 (owner-directed) — Build the real Holdings model, closing the gap BQ-009 flagged.

## What changed

- **`backend/app/models/holding.py`** — new `Holding` model: `id`, `user_id` (loose UUID, no FK — same
  pattern as Income/Goal), `product_type` (plain string, not a DB enum), `alias`, `display_name` (nullable,
  the real product/institution name — never sent to the LLM), `characteristics` (JSONB). Unique constraint
  on `(user_id, alias)`.
- **D-044 (Tier 1) — two implementation choices, owner-confirmed before code was written:**
  1. Single table + JSONB `characteristics`, not one child table per D-013's 8 types — matches the flat
     shape already used in `docs/fixtures/FIXTURE_user_01.json`.
  2. `product_type` left unconstrained (plain string) rather than a DB enum, so as not to silently resolve
     `PROJECT_SPEC.md` §8's open `savings_balance` taxonomy question as a side effect of this build task.
- **`GoalFunding.holding_id` wired to a real FK** (`holdings.id`, `ON DELETE CASCADE`) — the exact item
  D-043 deferred until Holdings existed.
- **Alembic migration `a6cd8d30a707`** generated, reviewed, and applied against the live Supabase DB. Fixed
  an autogenerate bug along the way: the generated `downgrade()` passed `None` as the FK constraint name to
  `drop_constraint`, which would fail if ever executed — named the constraint explicitly in both directions.
- **`docs/BUILD_QUEUE.md`** updated: new BQ-012 entry logged straight to DONE (owner-directed mid-session,
  traces to D-010/D-011/D-013/D-044); BQ-010 updated to reflect it's now fully unblocked.

## Verification

- App imports cleanly with all four models (`Income`, `Goal`, `GoalFunding`, `Holding`) registered.
- Migration applied against the live DB with no errors.
- Round-trip test against the real database: created a `Holding`, a `Goal`, and a `GoalFunding` linking
  them; confirmed the FK's `ON DELETE CASCADE` actually fires — deleting the `Holding` removed the
  `GoalFunding` row.
- `/health` → 200, `/health/db` → 200 with all four models loaded.

## Why

BQ-009 surfaced that `Goal.funded_by.holding_id` had nothing to reference — Holdings didn't exist anywhere
in the codebase. Rather than leave that as a loose reference indefinitely, the owner chose to build the
real Holdings model now, which also fully unblocks BQ-010 (live budget computation needs `Holding`'s
EMI/SIP/premium characteristic fields to read from).

## What's next

- **BQ-010** — Implement live budget computation. Now fully unblocked (Income, Goal, and Holdings all
  exist). Good candidate for the next session.
- Standing open items, unchanged: legal review of D-009, data privacy policy (D-010), BQ-004 (blocked on
  the `deepen`-selection rule), Decision 2 (per-item management depth), the D-013 idle-cash/`savings_balance`
  taxonomy question (still open — deliberately not resolved by this session's `product_type` choice).
