# Session — 02-Aug-2026 (third session)

**Task:** Write the Tier-3 brief for FINDING 9 and FINDING 10 (flagged for thinking-home in the last build
session, still unresolved when D-033/D-034 shifted focus to the operating model).

**What changed:**
- Added `docs/BRIEF-004_gap_surfacing_scope.md` — full Tier-3 brief per `DECISION_PROTOCOL.md` §5.2. Central
  question: does D-032's on-topic gap-surfacing rule need to widen given FINDING 10 (unprompted, mid-answer
  term-insurance surfacing on an on-topic question, 1/5 runs), and can any fix avoid worsening FINDING 9
  (Card-1 dropped entirely, 2/5 runs)? Surfaced a genuine interpretive question the build session's own
  writeup didn't fully separate out: whether FINDING 10 is D-032's pre-registered falsification (same channel
  as FINDING 8, failing twice) or a new channel (mid-answer insertion vs. the closing-offer channel D-032's
  rule actually governs) — this determines whether a narrow patch is legitimate or whether P-002's
  twice-failed-channel bar has already been met. Three paths modeled (narrow patch / D-032's own reserved
  broader gate / backend-architectural `surfaceable_gaps` field), none chosen — owner's call per protocol.
- `docs/DECISION_LOG.md`: appended the BRIEF-004 escalation summary (status, trigger, question, paths,
  what only the owner can judge, rule extraction), pointing to the full brief file.
- `PROJECT_SPEC.md` §8: added the open item, flagged as blocking further §2/§3 prompt work until resolved.

**Why this isn't resolved here:** both findings touch the compliance line (D-009 → D-025 → D-032's lineage),
which is squarely inside the two things D-034's autonomy grant does not cover. This is exactly what that
carve-out is for — a brief gets written and handed to the owner, not auto-decided.

**What's next:** Owner reviews BRIEF-004 and picks a path (or asks for a different one — the brief doesn't
recommend). Once decided, the fix gets written into `TEACHING_SYSTEM_PROMPT.md` §2/§3, regenerated as a new
runnable prompt version, and ideally re-run against the same n=5 repeat setup before being treated as closed
— consistent with how D-025/D-028/D-032 each closed only after a real run confirmed the fix held.
`docs/BUILD_QUEUE.md` READY stays empty in the meantime; BQ-004 stays BLOCKED (unrelated).
