# Session 2026-08-03g

**What:** Owner reviewed BRIEF-006 (deepen-selection logic, escalated last session), asked for my
read, then confirmed it: **Path C — defer.** Logged as **D-049**
(`docs/decisions/D-049-deepen-selection-deferred.md`, indexed in `docs/DECISION_LOG.md` per D-046's
format). Reasoning it captures: Phase 1 testing already simulates `deepen` via D-028's fixture stub
so nothing is blocked by deferring; the narrow-classifier path (reusing D-002's Haiku split) doesn't
actually satisfy D-028's own "auditable in code" justification, it just relocates the judgment to a
smaller model; the immediately-buildable mechanical path (text/alias matching) would rarely fire on
real questions and converges on "deepen nothing" anyway; and `app/` is still empty, so any rule
built now is designed against a guessed input shape.

This does not settle BRIEF-006's underlying regulatory question (does a narrower model call satisfy
an architectural guarantee) — that stays open for whenever `app/` has a real conversation interface
and this gets picked back up.

**Also touched:**
- `docs/BUILD_QUEUE.md` — BQ-004 re-scoped: blocked on a real conversation interface existing, not
  merely on an undecided rule.
- `PROJECT_SPEC.md` §8 — the deepen-selection open item annotated with D-049's outcome (spec changed
  this session — flagging per CLAUDE.md's end-of-session step).

**What's next:** Build queue's READY list is still empty; BQ-004 stays blocked on `app/`
infrastructure that doesn't exist yet. Next session needs either a new build item queued, or a
different strategic question from the owner.
