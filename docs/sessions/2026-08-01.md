# Session — 01-Aug-2026

**Task:** BQ-005 — regenerate the runnable system prompt to v0.5 (D-029 provenance rule).

**What changed:**
- Added `docs/prompts/SYSTEM_PROMPT_v0_5_runnable.md`, assembled from the current
  `docs/prompts/TEACHING_SYSTEM_PROMPT.md` following the v0.4 assembly pattern. §2 gains rule 5 (provenance —
  a number sourced from the profile vs. a general market-typical figure must never look the same to the
  user; typical figures are always a range, never a point estimate). §5 gains the typical-figure phrasing
  example (wrong/wrong/right). Owner-facing annotations and the P5 design-note HTML comment stripped, per
  BQ-005's instructions.
- Diff was shown to the owner before writing, per session request; approved as-is, no changes needed.
- `docs/BUILD_QUEUE.md`: moved BQ-005 from READY to DONE.

**What's next:** BQ-001 (run Q7/Q8 against v0.5) is next in READY. BQ-002/BQ-003 also open; BQ-004 stays
BLOCKED pending thinking-home decision on backend `deepen` selection logic.

**Sync note:** No `PROJECT_SPEC.md` or `docs/DECISION_LOG.md` changes this session — only `docs/BUILD_QUEUE.md`
and the new prompt file, both build-home-only artifacts. Nothing needs re-uploading to the Claude Project.

(Note: `PROJECT_SPEC.md` has a pre-existing uncommitted modification from an earlier session, unrelated to
BQ-005 — left untouched and uncommitted here.)
