# Session — 02-Aug-2026 (seventh session)

**Task:** Log the owner's decision on BRIEF-005.

**What changed:**
- Logged **D-036** in `docs/DECISION_LOG.md`: BRIEF-005 resolved, no fix. Owner judged "worth X" bridging
  language without an attached comparative/ordering word does not cross §3 rule 5's true-vs-attend line —
  explicitly confirmed this covers the two verbatim "worth having in view" instances, not only the gentler
  variants, after that distinction was raised rather than left implicit.
- D-025's named FAIL example ("worth having in view first") is narrowed: the operative failure was "first"
  (an ordering claim), not the phrase alone. Comparative/ordering "worth X" phrasing remains forbidden —
  only the ordering-word-free form is cleared.
- `PROJECT_SPEC.md` §8: BRIEF-005 marked resolved; v2.5 change log entry added.
- No prompt files changed — this decision doesn't touch `TEACHING_SYSTEM_PROMPT.md` or produce a new
  runnable version.

**Why the clarifying check mattered:** my own plain-language walkthrough of the brief initially illustrated
the "softer case" argument with "worth noting" rather than the verbatim "worth having in view" instances,
which could have let the owner decide without realizing the exact banned phrase was included in what they
were clearing. Flagged it before logging rather than after — the decision as logged is the one actually made
with full information, not the one that would've been logged from an underspecified framing.

**What's next:** FINDING 9 (Card-1 omission) is now the only open finding from the BQ-007 series without a
brief. Standing open items unchanged: Decision 3, BQ-004's selection rule, the D-013 idle-cash question,
legal review, data privacy policy.
