# Copy to app/.env and fill in real values (gitignored via the repo-root .gitignore's .env/.env.* rules).
# Get these from the Supabase dashboard -> Project Settings -> API (project: fintutor-dev, D-008).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Defaults to http://localhost:8000 if unset — override for a physical device or non-default port.
EXPO_PUBLIC_BACKEND_URL=
